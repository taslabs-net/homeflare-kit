// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-openbao, generated — see fetch-specs.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  OpenBaoProtocol,
  type OpenBaoOpError,
  type OpenBaoOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { OpenBaoOpError, OpenBaoOpContext };

export interface StepDownLeaderRequest {}
export const StepDownLeaderRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "POST", uri: "/sys/step-down", code: 200 }),
  ),
).annotate({
  identifier: "StepDownLeaderRequest",
}) as any as S.Schema<StepDownLeaderRequest>;

export interface StepDownLeaderResponse {}
export const StepDownLeaderResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "StepDownLeaderResponse",
}) as any as S.Schema<StepDownLeaderResponse>;

export type StepDownLeaderError = OpenBaoOpError;
/** Cause the node to give up active status. This endpoint forces the node to give up active status. If the node does not have active status, this endpoint does nothing. Note that the node will sleep for ten seconds before attempting to grab the active lock again, but if no standby nodes grab the active lock in the interim, the same node may become the active node again. */
export const stepDownLeader: API.OperationMethod<
  StepDownLeaderRequest,
  StepDownLeaderResponse,
  StepDownLeaderError,
  OpenBaoOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StepDownLeaderRequest,
  output: StepDownLeaderResponse,
  errors: [],
  protocol: OpenBaoProtocol,
  retry: Retry.Retry,
}));
