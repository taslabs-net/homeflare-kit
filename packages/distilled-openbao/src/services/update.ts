// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-openbao, generated — see fetch-specs.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  OpenBaoProtocol,
  type OpenBaoOpError,
  type OpenBaoOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { OpenBaoOpError, OpenBaoOpContext };

export interface UpdateGenerationAttemptRootTokenRequest {
  /** Unseal key share. */
  key: string;
  /** Nonce for the generation operation. */
  nonce: string;
}
export const UpdateGenerationAttemptRootTokenRequest = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      key: S.String,
      nonce: S.String,
    }).pipe(
      T.Http({
        method: "POST",
        uri: "/sys/generate-root-token/update",
        code: 200,
      }),
    ),
).annotate({
  identifier: "UpdateGenerationAttemptRootTokenRequest",
}) as any as S.Schema<UpdateGenerationAttemptRootTokenRequest>;

export interface UpdateGenerationAttemptRootTokenResponse {
  complete?: boolean;
  encoded_token?: string;
  nonce?: string;
  pgp_fingerprint?: string;
  progress?: number;
  required?: number;
  started?: boolean;
}
export const UpdateGenerationAttemptRootTokenResponse = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      complete: S.optional(S.Boolean),
      encoded_token: S.optional(S.String),
      nonce: S.optional(S.String),
      pgp_fingerprint: S.optional(S.String),
      progress: S.optional(S.Number),
      required: S.optional(S.Number),
      started: S.optional(S.Boolean),
    }),
).annotate({
  identifier: "UpdateGenerationAttemptRootTokenResponse",
}) as any as S.Schema<UpdateGenerationAttemptRootTokenResponse>;

export type UpdateGenerationAttemptRootTokenError = OpenBaoOpError;
/** Provide an unseal key share for root token generation. If the threshold number of unseal key shares is reached, OpenBao will complete the root generation and issue the new token. Otherwise, this API must be called multiple times until that threshold is met. The attempt nonce must be provided with each call. */
export const updateGenerationAttemptRootToken: API.OperationMethod<
  UpdateGenerationAttemptRootTokenRequest,
  UpdateGenerationAttemptRootTokenResponse,
  UpdateGenerationAttemptRootTokenError,
  OpenBaoOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateGenerationAttemptRootTokenRequest,
  output: UpdateGenerationAttemptRootTokenResponse,
  errors: [],
  protocol: OpenBaoProtocol,
  retry: Retry.Retry,
}));
