// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-openbao, generated — see fetch-specs.ts). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  OpenBaoProtocol,
  type OpenBaoOpError,
  type OpenBaoOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { OpenBaoOpError, OpenBaoOpContext };

/** Specifies an array of PGP public keys used to encrypt the output unseal keys. Ordering is preserved. The keys must be base64-encoded from their original binary representation. The size of this array must be the same as `secret_shares`. */
export type InitializeSystemRequestPgpKeysList = Array<string>;
export const InitializeSystemRequestPgpKeysList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<InitializeSystemRequestPgpKeysList>;

/** Specifies an array of PGP public keys used to encrypt the output recovery keys. Ordering is preserved. The keys must be base64-encoded from their original binary representation. The size of this array must be the same as `recovery_shares`. */
export type InitializeSystemRequestRecoveryPgpKeysList = Array<string>;
export const InitializeSystemRequestRecoveryPgpKeysList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<InitializeSystemRequestRecoveryPgpKeysList>;

export interface InitializeSystemRequest {
  /** Specifies an array of PGP public keys used to encrypt the output unseal keys. Ordering is preserved. The keys must be base64-encoded from their original binary representation. The size of this array must be the same as `secret_shares`. */
  pgp_keys?: InitializeSystemRequestPgpKeysList;
  /** Specifies an array of PGP public keys used to encrypt the output recovery keys. Ordering is preserved. The keys must be base64-encoded from their original binary representation. The size of this array must be the same as `recovery_shares`. */
  recovery_pgp_keys?: InitializeSystemRequestRecoveryPgpKeysList;
  /** Specifies the number of shares to split the recovery key into. */
  recovery_shares?: number;
  /** Specifies the number of shares required to reconstruct the recovery key. This must be less than or equal to `recovery_shares`. */
  recovery_threshold?: number;
  /** Specifies a PGP public key used to encrypt the initial root token. The key must be base64-encoded from its original binary representation. */
  root_token_pgp_key?: string;
  /** Specifies the number of shares to split the unseal key into. */
  secret_shares?: number;
  /** Specifies the number of shares required to reconstruct the unseal key. This must be less than or equal secret_shares. If using OpenBao HSM with auto-unsealing, this value must be the same as `secret_shares`. */
  secret_threshold?: number;
}
export const InitializeSystemRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pgp_keys: S.optional(InitializeSystemRequestPgpKeysList),
    recovery_pgp_keys: S.optional(InitializeSystemRequestRecoveryPgpKeysList),
    recovery_shares: S.optional(S.Number),
    recovery_threshold: S.optional(S.Number),
    root_token_pgp_key: S.optional(S.String),
    secret_shares: S.optional(S.Number),
    secret_threshold: S.optional(S.Number),
  }).pipe(T.Http({ method: "POST", uri: "/sys/init", code: 200 })),
).annotate({
  identifier: "InitializeSystemRequest",
}) as any as S.Schema<InitializeSystemRequest>;

export interface InitializeSystemResponse {}
export const InitializeSystemResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "InitializeSystemResponse",
}) as any as S.Schema<InitializeSystemResponse>;

export type InitializeSystemError = OpenBaoOpError;
/** Initialize a new OpenBao instance. The OpenBao instance must not have been previously initialized. The recovery options are only available when using Auto Unseal. */
export const initializeSystem: API.OperationMethod<
  InitializeSystemRequest,
  InitializeSystemResponse,
  InitializeSystemError,
  OpenBaoOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InitializeSystemRequest,
  output: InitializeSystemResponse,
  errors: [],
  protocol: OpenBaoProtocol,
  retry: Retry.Retry,
}));
