// AUTO-GENERATED by scripts/generate.ts from .generated-specs (pinned litellm-openapi @ 1.100.0 — see README). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  LitellmProtocol,
  type LitellmOpError,
  type LitellmOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { LitellmOpError, LitellmOpContext };

export interface PostRagIngestRagIngestRequest {}
export const PostRagIngestRagIngestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "POST", uri: "/rag/ingest", code: 200 })),
).annotate({
  identifier: "PostRagIngestRagIngestRequest",
}) as any as S.Schema<PostRagIngestRagIngestRequest>;

export interface PostRagIngestRagIngestResponse {
  body: unknown;
}
export const PostRagIngestRagIngestResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: S.Unknown,
  }),
).annotate({
  identifier: "PostRagIngestRagIngestResponse",
}) as any as S.Schema<PostRagIngestRagIngestResponse>;

export interface PostRagIngestV1RagIngestRequest {}
export const PostRagIngestV1RagIngestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "POST", uri: "/v1/rag/ingest", code: 200 }),
  ),
).annotate({
  identifier: "PostRagIngestV1RagIngestRequest",
}) as any as S.Schema<PostRagIngestV1RagIngestRequest>;

export interface PostRagIngestV1RagIngestResponse {
  body: unknown;
}
export const PostRagIngestV1RagIngestResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: S.Unknown,
  }),
).annotate({
  identifier: "PostRagIngestV1RagIngestResponse",
}) as any as S.Schema<PostRagIngestV1RagIngestResponse>;

export interface PostRagQueryRagQueryRequest {}
export const PostRagQueryRagQueryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "POST", uri: "/rag/query", code: 200 })),
).annotate({
  identifier: "PostRagQueryRagQueryRequest",
}) as any as S.Schema<PostRagQueryRagQueryRequest>;

export interface PostRagQueryRagQueryResponse {
  body: unknown;
}
export const PostRagQueryRagQueryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: S.Unknown,
  }),
).annotate({
  identifier: "PostRagQueryRagQueryResponse",
}) as any as S.Schema<PostRagQueryRagQueryResponse>;

export interface PostRagQueryV1RagQueryRequest {}
export const PostRagQueryV1RagQueryRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "POST", uri: "/v1/rag/query", code: 200 }),
  ),
).annotate({
  identifier: "PostRagQueryV1RagQueryRequest",
}) as any as S.Schema<PostRagQueryV1RagQueryRequest>;

export interface PostRagQueryV1RagQueryResponse {
  body: unknown;
}
export const PostRagQueryV1RagQueryResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    body: S.Unknown,
  }),
).annotate({
  identifier: "PostRagQueryV1RagQueryResponse",
}) as any as S.Schema<PostRagQueryV1RagQueryResponse>;

export type PostRagIngestRagIngestError = LitellmOpError;
/** Rag Ingest RAG Ingest endpoint - all-in-one document ingestion pipeline. Supports form upload (for files) or JSON body (for URLs). ## Form upload (for files): ```bash curl -X POST "http://localhost:4000/v1/rag/ingest" \ -H "Authorization: Bearer sk-1234" \ -F file="@document.pdf" \ -F 'ingest_options={"vector_store": {"custom_llm_provider": "openai"}}' ``` ## JSON body (for URLs): ```bash curl -X POST "http://localhost:4000/v1/rag/ingest" \ -H "Authorization: Bearer sk-1234" \ -H "Content-Type: application/json" \ -d '{ "file_url": "https://example.com/document.pdf", "ingest_options": {"vector_store": {"custom_llm_provider": "openai"}} }' ``` ## Bedrock: ```bash curl -X POST "http://localhost:4000/v1/rag/ingest" \ -H "Authorization: Bearer sk-1234" \ -F file="@document.pdf" \ -F 'ingest_options={"vector_store": {"custom_llm_provider": "bedrock"}}' ``` */
export const postRagIngestRagIngest: API.OperationMethod<
  PostRagIngestRagIngestRequest,
  PostRagIngestRagIngestResponse,
  PostRagIngestRagIngestError,
  LitellmOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PostRagIngestRagIngestRequest,
  output: PostRagIngestRagIngestResponse,
  errors: [],
  protocol: LitellmProtocol,
  retry: Retry.Retry,
}));

export type PostRagIngestV1RagIngestError = LitellmOpError;
/** Rag Ingest RAG Ingest endpoint - all-in-one document ingestion pipeline. Supports form upload (for files) or JSON body (for URLs). ## Form upload (for files): ```bash curl -X POST "http://localhost:4000/v1/rag/ingest" \ -H "Authorization: Bearer sk-1234" \ -F file="@document.pdf" \ -F 'ingest_options={"vector_store": {"custom_llm_provider": "openai"}}' ``` ## JSON body (for URLs): ```bash curl -X POST "http://localhost:4000/v1/rag/ingest" \ -H "Authorization: Bearer sk-1234" \ -H "Content-Type: application/json" \ -d '{ "file_url": "https://example.com/document.pdf", "ingest_options": {"vector_store": {"custom_llm_provider": "openai"}} }' ``` ## Bedrock: ```bash curl -X POST "http://localhost:4000/v1/rag/ingest" \ -H "Authorization: Bearer sk-1234" \ -F file="@document.pdf" \ -F 'ingest_options={"vector_store": {"custom_llm_provider": "bedrock"}}' ``` */
export const postRagIngestV1RagIngest: API.OperationMethod<
  PostRagIngestV1RagIngestRequest,
  PostRagIngestV1RagIngestResponse,
  PostRagIngestV1RagIngestError,
  LitellmOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PostRagIngestV1RagIngestRequest,
  output: PostRagIngestV1RagIngestResponse,
  errors: [],
  protocol: LitellmProtocol,
  retry: Retry.Retry,
}));

export type PostRagQueryRagQueryError = LitellmOpError;
/** Rag Query RAG Query endpoint - search vector store, optionally rerank, and generate LLM response. This endpoint: 1. Extracts the query from the last user message 2. Searches the vector store for relevant context 3. Optionally reranks the results 4. Generates an LLM response with the retrieved context ## Example Request: ```bash curl -X POST "http://localhost:4000/v1/rag/query" \ -H "Authorization: Bearer sk-1234" \ -H "Content-Type: application/json" \ -d '{ "model": "gpt-4o-mini", "messages": [{"role": "user", "content": "What is LiteLLM?"}], "retrieval_config": { "vector_store_id": "vs_abc123", "custom_llm_provider": "openai", "top_k": 5 } }' ``` ## With Reranking: ```bash curl -X POST "http://localhost:4000/v1/rag/query" \ -H "Authorization: Bearer sk-1234" \ -H "Content-Type: application/json" \ -d '{ "model": "gpt-4o-mini", "messages": [{"role": "user", "content": "What is LiteLLM?"}], "retrieval_config": { "vector_store_id": "vs_abc123", "custom_llm_provider": "openai", "top_k": 10 }, "rerank": { "enabled": true, "model": "cohere/rerank-english-v3.0", "top_n": 3 } }' ``` */
export const postRagQueryRagQuery: API.OperationMethod<
  PostRagQueryRagQueryRequest,
  PostRagQueryRagQueryResponse,
  PostRagQueryRagQueryError,
  LitellmOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PostRagQueryRagQueryRequest,
  output: PostRagQueryRagQueryResponse,
  errors: [],
  protocol: LitellmProtocol,
  retry: Retry.Retry,
}));

export type PostRagQueryV1RagQueryError = LitellmOpError;
/** Rag Query RAG Query endpoint - search vector store, optionally rerank, and generate LLM response. This endpoint: 1. Extracts the query from the last user message 2. Searches the vector store for relevant context 3. Optionally reranks the results 4. Generates an LLM response with the retrieved context ## Example Request: ```bash curl -X POST "http://localhost:4000/v1/rag/query" \ -H "Authorization: Bearer sk-1234" \ -H "Content-Type: application/json" \ -d '{ "model": "gpt-4o-mini", "messages": [{"role": "user", "content": "What is LiteLLM?"}], "retrieval_config": { "vector_store_id": "vs_abc123", "custom_llm_provider": "openai", "top_k": 5 } }' ``` ## With Reranking: ```bash curl -X POST "http://localhost:4000/v1/rag/query" \ -H "Authorization: Bearer sk-1234" \ -H "Content-Type: application/json" \ -d '{ "model": "gpt-4o-mini", "messages": [{"role": "user", "content": "What is LiteLLM?"}], "retrieval_config": { "vector_store_id": "vs_abc123", "custom_llm_provider": "openai", "top_k": 10 }, "rerank": { "enabled": true, "model": "cohere/rerank-english-v3.0", "top_n": 3 } }' ``` */
export const postRagQueryV1RagQuery: API.OperationMethod<
  PostRagQueryV1RagQueryRequest,
  PostRagQueryV1RagQueryResponse,
  PostRagQueryV1RagQueryError,
  LitellmOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PostRagQueryV1RagQueryRequest,
  output: PostRagQueryV1RagQueryResponse,
  errors: [],
  protocol: LitellmProtocol,
  retry: Retry.Retry,
}));
