// AUTO-GENERATED by scripts/generate.ts from .generated-specs (pinned litellm-openapi @ 1.100.0 — see README). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  LitellmProtocol,
  type LitellmOpError,
  type LitellmOpContext,
} from "../protocol.ts";
import * as Retry from "../retry.ts";

export type { LitellmOpError, LitellmOpContext };

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

/** How to locate the upstream agent card. String-valued so it serializes cleanly over JSON / Pydantic. */
export type DiscoveryMode = "well_known_fallback" | "langgraph_platform";
export const DiscoveryMode = S.String;

export type PostDiscoverAgentCardV1A2aDiscoverRequestParamsMap = {
  [key: string]: unknown | undefined;
};
export const PostDiscoverAgentCardV1A2aDiscoverRequestParamsMap =
  /*@__PURE__*/ S.Record(
    S.String,
    S.Unknown,
  ) as any as S.Schema<PostDiscoverAgentCardV1A2aDiscoverRequestParamsMap>;

export interface PostDiscoverAgentCardV1A2aDiscoverRequest {
  /** How to locate the upstream card. ``well_known_fallback`` for pure A2A agents (try standard paths); ``langgraph_platform`` for LangGraph Platform deployments where the card is shared across assistants and disambiguated by a query parameter. */
  discovery_mode?: DiscoveryMode | (string & {});
  /** Mode-specific parameters. ``langgraph_platform`` requires ``{'assistant_id': <id>}``. ``well_known_fallback`` ignores this. */
  params?: PostDiscoverAgentCardV1A2aDiscoverRequestParamsMap | null;
  /** Base URL of the upstream agent. Behavior depends on ``discovery_mode``: ``well_known_fallback`` (default) tries /.well-known/agent-card.json, /.well-known/agent.json, /agent.json under this URL in order; ``langgraph_platform`` hits ``/.well-known/agent-card.json?assistant_id=<id>`` instead. */
  url: string;
}
export const PostDiscoverAgentCardV1A2aDiscoverRequest =
  /*@__PURE__*/ S.suspend(() =>
    S.Struct({
      discovery_mode: S.optional(DiscoveryMode),
      params: S.optional(
        S.NullOr(PostDiscoverAgentCardV1A2aDiscoverRequestParamsMap),
      ),
      url: S.String,
    }).pipe(T.Http({ method: "POST", uri: "/v1/a2a/discover", code: 200 })),
  ).annotate({
    identifier: "PostDiscoverAgentCardV1A2aDiscoverRequest",
  }) as any as S.Schema<PostDiscoverAgentCardV1A2aDiscoverRequest>;

export type DiscoverAgentResponseAgentCardMap = {
  [key: string]: unknown | undefined;
};
export const DiscoverAgentResponseAgentCardMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<DiscoverAgentResponseAgentCardMap>;

export interface DiscoverAgentResponse {
  agent_card: DiscoverAgentResponseAgentCardMap;
  url: string;
}
export const DiscoverAgentResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    agent_card: DiscoverAgentResponseAgentCardMap,
    url: S.String,
  }),
).annotate({
  identifier: "DiscoverAgentResponse",
}) as any as S.Schema<DiscoverAgentResponse>;

export type PostDiscoverAgentCardV1A2aDiscoverError =
  | UnprocessableEntity
  | LitellmOpError;
/** Discover Agent Card Fetch the upstream agent's well-known card so the UI can show the admin which skills/capabilities the agent exposes. Only proxy admins can call this — the UI uses it during agent registration, and we don't want arbitrary keys probing internal URLs. Example: ```bash curl -X POST "http://localhost:4000/v1/a2a/discover" \ -H "Authorization: Bearer <admin_key>" \ -H "Content-Type: application/json" \ -d '{"url": "https://upstream-agent.example.com"}' ``` */
export const postDiscoverAgentCardV1A2aDiscover: API.OperationMethod<
  PostDiscoverAgentCardV1A2aDiscoverRequest,
  DiscoverAgentResponse,
  PostDiscoverAgentCardV1A2aDiscoverError,
  LitellmOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PostDiscoverAgentCardV1A2aDiscoverRequest,
  output: DiscoverAgentResponse,
  errors: [UnprocessableEntity],
  protocol: LitellmProtocol,
  retry: Retry.Retry,
}));
