// AUTO-GENERATED by scripts/generate.ts from .generated-specs (hand-authored — see scripts/convert.ts and docs/provenance.md). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import {
  CaddyProtocol,
  type CaddyOpError,
  type CaddyOpContext,
} from "../protocol.ts";
import { UnknownCaddyError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { CaddyOpError, CaddyOpContext };

export interface AdaptRequest {
  /** The config, in the format `contentType` names. */
  config: unknown;
  /** The adapter to use, as `text/<adapter>` (e.g. `text/caddyfile`) — omit, or `application/json`, for Caddy's native JSON. "This endpoint supports different config formats using config adapters" (docs, POST /load). */
  contentType?: string;
}
export const AdaptRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    config: S.Unknown.pipe(T.HttpBody()),
    contentType: S.optional(S.String.pipe(T.Header("Content-Type"))),
  }).pipe(T.Http({ method: "POST", uri: "/adapt", code: 200 })),
).annotate({ identifier: "AdaptRequest" }) as any as S.Schema<AdaptRequest>;

export interface Warning {
  /** The source file, when known. */
  file?: string;
  /** The 1-based line number, when known. */
  line?: number;
  /** What the adapter is warning about. */
  message?: string;
}
export const Warning = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    file: S.optional(S.String),
    line: S.optional(S.Number),
    message: S.optional(S.String),
  }),
).annotate({ identifier: "Warning" }) as any as S.Schema<Warning>;

/** Adapter warnings, in the order Caddy reported them. */
export type WarningList = Array<Warning>;
export const WarningList = /*@__PURE__*/ S.Array(
  Warning,
) as any as S.Schema<WarningList>;

export interface AdaptResponse {
  /** The adapted config, as Caddy's native JSON. */
  result?: unknown;
  /** Adapter warnings — formatting, deprecated syntax, etc. */
  warnings?: WarningList;
}
export const AdaptResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    result: S.optional(S.Unknown),
    warnings: S.optional(WarningList),
  }),
).annotate({ identifier: "AdaptResponse" }) as any as S.Schema<AdaptResponse>;

export interface CaCertificatesRequest {
  /** The CA's id — "local" is Caddy's always-available default CA. */
  id: string;
}
export const CaCertificatesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "GET", uri: "/pki/ca/{id}/certificates", code: 200 }),
  ),
).annotate({
  identifier: "CaCertificatesRequest",
}) as any as S.Schema<CaCertificatesRequest>;

export interface CaCertificatesResponse {
  /** The intermediate + root certificate chain, PEM-encoded. */
  pem?: string;
}
export const CaCertificatesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pem: S.optional(S.String),
  }),
).annotate({
  identifier: "CaCertificatesResponse",
}) as any as S.Schema<CaCertificatesResponse>;

export interface CaInfoRequest {
  /** The CA's id — "local" is Caddy's always-available default CA. */
  id: string;
}
export const CaInfoRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/pki/ca/{id}", code: 200 })),
).annotate({ identifier: "CaInfoRequest" }) as any as S.Schema<CaInfoRequest>;

export interface CaInfoResponse {
  /** The CA's id. */
  id?: string;
  /** The CA's display name. */
  name?: string;
  /** The root certificate's CN. */
  root_common_name?: string;
  /** The intermediate certificate's CN. */
  intermediate_common_name?: string;
  /** The root certificate, PEM-encoded. */
  root_certificate?: string;
  /** The intermediate certificate, PEM-encoded. */
  intermediate_certificate?: string;
}
export const CaInfoResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    name: S.optional(S.String),
    root_common_name: S.optional(S.String),
    intermediate_common_name: S.optional(S.String),
    root_certificate: S.optional(S.String),
    intermediate_certificate: S.optional(S.String),
  }),
).annotate({ identifier: "CaInfoResponse" }) as any as S.Schema<CaInfoResponse>;

export interface MutateConfigRequest {
  /** Slash-delimited path into the config tree (e.g. "apps/http/servers/myserver/listen"), or "" for the whole config. */
  path: string;
  /** The value to set/create/replace at `path`. */
  value: unknown;
  /** The `Etag` from a prior `GetConfig` at this same path — optimistic concurrency control (docs, "Concurrent config changes"). A stale value answers 412. */
  ifMatch?: string;
  /** Set to the literal `"must-revalidate"` to force a reload even when the new value is identical to what is already there. */
  cacheControl?: string;
}
export const MutateConfigRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String.pipe(T.Label()),
    value: S.Unknown.pipe(T.HttpBody()),
    ifMatch: S.optional(S.String.pipe(T.Header("If-Match"))),
    cacheControl: S.optional(S.String.pipe(T.Header("Cache-Control"))),
  }).pipe(T.Http({ method: "POST", uri: "/config/{path}", code: 200 })),
).annotate({
  identifier: "MutateConfigRequest",
}) as any as S.Schema<MutateConfigRequest>;

export interface CreateConfigResponse {}
export const CreateConfigResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "CreateConfigResponse",
}) as any as S.Schema<CreateConfigResponse>;

export interface DeleteConfigRequest {
  /** Slash-delimited path into the config tree (e.g. "apps/http/servers/myserver/listen"), or "" for the whole config. */
  path: string;
  /** The `Etag` from a prior `GetConfig` at this same path — optimistic concurrency control (docs, "Concurrent config changes"). A stale value answers 412. */
  ifMatch?: string;
}
export const DeleteConfigRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String.pipe(T.Label()),
    ifMatch: S.optional(S.String.pipe(T.Header("If-Match"))),
  }).pipe(T.Http({ method: "DELETE", uri: "/config/{path}", code: 200 })),
).annotate({
  identifier: "DeleteConfigRequest",
}) as any as S.Schema<DeleteConfigRequest>;

export interface DeleteConfigResponse {}
export const DeleteConfigResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteConfigResponse",
}) as any as S.Schema<DeleteConfigResponse>;

export interface GetConfigRequest {
  /** Slash-delimited path into the config tree (e.g. "apps/http/servers/myserver/listen"), or "" for the whole config. */
  path: string;
}
export const GetConfigRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/config/{path}", code: 200 })),
).annotate({
  identifier: "GetConfigRequest",
}) as any as S.Schema<GetConfigRequest>;

export interface GetConfigResponse {
  /** The config value at `path`. */
  value?: unknown;
  /** Path + content hash, for `If-Match` on a later mutation. */
  etag?: string;
}
export const GetConfigResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    value: S.optional(S.Unknown),
    etag: S.optional(S.String.pipe(T.Header("Etag"))),
  }),
).annotate({
  identifier: "GetConfigResponse",
}) as any as S.Schema<GetConfigResponse>;

export interface IdConfigRequest {
  /** The `"@id"` value embedded in the config JSON. */
  id: string;
  /** Further traversal past the `"@id"` scope, or "" to stop there (e.g. "upstreams" for a reverse_proxy handler's `@id`). */
  path: string;
}
export const IdConfigRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/id/{id}/{path}", code: 200 })),
).annotate({
  identifier: "IdConfigRequest",
}) as any as S.Schema<IdConfigRequest>;

export interface GetReverseProxyUpstreamsRequest {}
export const GetReverseProxyUpstreamsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "GET", uri: "/reverse_proxy/upstreams", code: 200 }),
  ),
).annotate({
  identifier: "GetReverseProxyUpstreamsRequest",
}) as any as S.Schema<GetReverseProxyUpstreamsRequest>;

export interface Upstream {
  /** The upstream's dial address. */
  address?: string;
  /** Active requests currently being handled by this upstream. */
  num_requests?: number;
  /** Failed requests remembered, per passive health checks. */
  fails?: number;
}
export const Upstream = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.optional(S.String),
    num_requests: S.optional(S.Number),
    fails: S.optional(S.Number),
  }),
).annotate({ identifier: "Upstream" }) as any as S.Schema<Upstream>;

export type UpstreamList = Array<Upstream>;
export const UpstreamList = /*@__PURE__*/ S.Array(
  Upstream,
) as any as S.Schema<UpstreamList>;

export type GetReverseProxyUpstreamsResponse = UpstreamList;
export const GetReverseProxyUpstreamsResponse = /*@__PURE__*/ S.suspend(() =>
  UpstreamList.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "GetReverseProxyUpstreamsResponse",
}) as any as S.Schema<GetReverseProxyUpstreamsResponse>;

export interface LoadRequest {
  /** The config, in the format `contentType` names. */
  config: unknown;
  /** The adapter to use, as `text/<adapter>` (e.g. `text/caddyfile`) — omit, or `application/json`, for Caddy's native JSON. "This endpoint supports different config formats using config adapters" (docs, POST /load). */
  contentType?: string;
  /** The file this config came from, so `--resume` keeps tracking it across a reload. */
  sourceFile?: string;
  /** The adapter name for `sourceFile` (e.g. "caddyfile"). */
  sourceAdapter?: string;
  /** Set to the literal `"must-revalidate"` to force a reload even when the new config is identical to the running one. */
  cacheControl?: string;
}
export const LoadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    config: S.Unknown.pipe(T.HttpBody()),
    contentType: S.optional(S.String.pipe(T.Header("Content-Type"))),
    sourceFile: S.optional(S.String.pipe(T.Header("Caddy-Config-Source-File"))),
    sourceAdapter: S.optional(
      S.String.pipe(T.Header("Caddy-Config-Source-Adapter")),
    ),
    cacheControl: S.optional(S.String.pipe(T.Header("Cache-Control"))),
  }).pipe(T.Http({ method: "POST", uri: "/load", code: 200 })),
).annotate({ identifier: "LoadRequest" }) as any as S.Schema<LoadRequest>;

export interface LoadResponse {
  /** Adapter warnings (formatting, deprecations) even on a successful load. */
  warnings?: WarningList;
}
export const LoadResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    warnings: S.optional(WarningList),
  }),
).annotate({ identifier: "LoadResponse" }) as any as S.Schema<LoadResponse>;

export interface ReplaceConfigResponse {}
export const ReplaceConfigResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ReplaceConfigResponse",
}) as any as S.Schema<ReplaceConfigResponse>;

export interface SetConfigResponse {}
export const SetConfigResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "SetConfigResponse",
}) as any as S.Schema<SetConfigResponse>;

export interface SetIdConfigRequest {
  /** The `"@id"` value embedded in the config JSON. */
  id: string;
  /** Further traversal past the `"@id"` scope, or "" to stop there. */
  path: string;
  /** The value to set/append at this scope. */
  value: unknown;
  /** The `Etag` from a prior `GetConfig` at this same path — optimistic concurrency control (docs, "Concurrent config changes"). A stale value answers 412. */
  ifMatch?: string;
  /** Set to the literal `"must-revalidate"` to force a reload even when the new value is identical to what is already there. */
  cacheControl?: string;
}
export const SetIdConfigRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Label()),
    value: S.Unknown.pipe(T.HttpBody()),
    ifMatch: S.optional(S.String.pipe(T.Header("If-Match"))),
    cacheControl: S.optional(S.String.pipe(T.Header("Cache-Control"))),
  }).pipe(T.Http({ method: "POST", uri: "/id/{id}/{path}", code: 200 })),
).annotate({
  identifier: "SetIdConfigRequest",
}) as any as S.Schema<SetIdConfigRequest>;

export interface SetConfigByIdResponse {}
export const SetConfigByIdResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "SetConfigByIdResponse",
}) as any as S.Schema<SetConfigByIdResponse>;

export interface StopServerRequest {}
export const StopServerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "POST", uri: "/stop", code: 200 })),
).annotate({
  identifier: "StopServerRequest",
}) as any as S.Schema<StopServerRequest>;

export interface StopServerResponse {}
export const StopServerResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "StopServerResponse",
}) as any as S.Schema<StopServerResponse>;

/** The list of allowed origins/hosts for API requests. Only needed if accessing the admin endpoint from a host different from the socket's network interface or if `enforce_origin` is true. If not set, the listener address will be the default value. If set but empty, no origins will be allowed. Enforced only on local (plaintext) endpoint. */
export type AdminConfigOrigins = Array<string>;
export const AdminConfigOrigins = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdminConfigOrigins>;

/** Options pertaining to configuration management. ConfigSettings configures the management of configuration. */
export interface ConfigSettings {
  /** Whether to keep a copy of the active config on disk. Default is true. Note that "pulled" dynamic configs (using the neighboring "load" module) are not persisted; only configs that are pushed to Caddy get persisted. */
  persist?: boolean;
  /** Loads a configuration to use. This is helpful if your configs are managed elsewhere, and you want Caddy to pull its config dynamically when it starts. The pulled config completely replaces the current one, just like any other config load. It is an error if a pulled config is configured to pull another config. EXPERIMENTAL: Subject to change. Module — the name is one of com.caddy.admin#CaddyConfigLoadersModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  load?: unknown;
}
export const ConfigSettings = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    persist: S.optional(S.Boolean),
    load: S.optional(S.Unknown),
  }),
).annotate({ identifier: "ConfigSettings" }) as any as S.Schema<ConfigSettings>;

/** List of names or IP addresses which refer to this server. Certificates will be obtained for these identifiers so secure TLS connections can be made using them. */
export type IdentityConfigIdentifiers = Array<string>;
export const IdentityConfigIdentifiers = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<IdentityConfigIdentifiers>;

/** Issuers that can provide this admin endpoint its identity certificate(s). Default: ACME issuers configured for ZeroSSL and Let's Encrypt. Be sure to change this if you require credentials for private identifiers. */
export type IdentityConfigIssuers = Array<unknown>;
export const IdentityConfigIssuers = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<IdentityConfigIssuers>;

/** Options that establish this server's identity. Identity refers to credentials which can be used to uniquely identify and authenticate this server instance. This is required if remote administration is enabled (but does not require remote administration to be enabled). Default: no identity management. IdentityConfig configures management of this server's identity. An identity consists of credentials that uniquely verify this instance; for example, TLS certificates (public + private key pairs). */
export interface IdentityConfig {
  /** List of names or IP addresses which refer to this server. Certificates will be obtained for these identifiers so secure TLS connections can be made using them. */
  identifiers?: IdentityConfigIdentifiers;
  /** Issuers that can provide this admin endpoint its identity certificate(s). Default: ACME issuers configured for ZeroSSL and Let's Encrypt. Be sure to change this if you require credentials for private identifiers. */
  issuers?: IdentityConfigIssuers;
}
export const IdentityConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    identifiers: S.optional(IdentityConfigIdentifiers),
    issuers: S.optional(IdentityConfigIssuers),
  }),
).annotate({ identifier: "IdentityConfig" }) as any as S.Schema<IdentityConfig>;

/** Base64-encoded DER certificates containing public keys to accept. (The contents of PEM certificate blocks are base64-encoded DER.) Any of these public keys can appear in any part of a verified chain. */
export type AdminAccessPublic_keys = Array<string>;
export const AdminAccessPublic_keys = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdminAccessPublic_keys>;

/** The API paths allowed. Paths are simple prefix matches. Any subpath of the specified paths will be allowed. */
export type AdminPermissionsPaths = Array<string>;
export const AdminPermissionsPaths = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdminPermissionsPaths>;

/** The HTTP methods allowed for the given paths. */
export type AdminPermissionsMethods = Array<string>;
export const AdminPermissionsMethods = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AdminPermissionsMethods>;

/** Limits what the associated identities are allowed to do. If unspecified, all permissions are granted. AdminPermissions specifies what kinds of requests are allowed to be made to the admin endpoint. */
export interface AdminPermissions {
  /** The API paths allowed. Paths are simple prefix matches. Any subpath of the specified paths will be allowed. */
  paths?: AdminPermissionsPaths;
  /** The HTTP methods allowed for the given paths. */
  methods?: AdminPermissionsMethods;
}
export const AdminPermissions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    paths: S.optional(AdminPermissionsPaths),
    methods: S.optional(AdminPermissionsMethods),
  }),
).annotate({
  identifier: "AdminPermissions",
}) as any as S.Schema<AdminPermissions>;

/** Limits what the associated identities are allowed to do. If unspecified, all permissions are granted. AdminPermissions specifies what kinds of requests are allowed to be made to the admin endpoint. */
export type AdminAccessPermissions = Array<AdminPermissions>;
export const AdminAccessPermissions = /*@__PURE__*/ S.Array(
  AdminPermissions,
) as any as S.Schema<AdminAccessPermissions>;

/** List of access controls for this secure admin endpoint. This configures TLS mutual authentication (i.e. authorized client certificates), but also application-layer permissions like which paths and methods each identity is authorized for. AdminAccess specifies what permissions an identity or group of identities are granted. */
export interface AdminAccess {
  /** Base64-encoded DER certificates containing public keys to accept. (The contents of PEM certificate blocks are base64-encoded DER.) Any of these public keys can appear in any part of a verified chain. */
  public_keys?: AdminAccessPublic_keys;
  /** Limits what the associated identities are allowed to do. If unspecified, all permissions are granted. AdminPermissions specifies what kinds of requests are allowed to be made to the admin endpoint. */
  permissions?: AdminAccessPermissions;
}
export const AdminAccess = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    public_keys: S.optional(AdminAccessPublic_keys),
    permissions: S.optional(AdminAccessPermissions),
  }),
).annotate({ identifier: "AdminAccess" }) as any as S.Schema<AdminAccess>;

/** List of access controls for this secure admin endpoint. This configures TLS mutual authentication (i.e. authorized client certificates), but also application-layer permissions like which paths and methods each identity is authorized for. AdminAccess specifies what permissions an identity or group of identities are granted. */
export type RemoteAdminAccess_control = Array<AdminAccess>;
export const RemoteAdminAccess_control = /*@__PURE__*/ S.Array(
  AdminAccess,
) as any as S.Schema<RemoteAdminAccess_control>;

/** Options pertaining to remote administration. By default, remote administration is disabled. If enabled, identity management must also be configured, as that is how the endpoint is secured. See the neighboring "identity" object. EXPERIMENTAL: This feature is subject to change. RemoteAdmin enables and configures remote administration. If enabled, a secure listener enforcing mutual TLS authentication will be started on a different port from the standard plaintext admin server. This endpoint is secured using identity management, which must be configured separately (because identity management does not depend on remote administration). See the admin/identity config struct. EXPERIMENTAL: Subject to change. */
export interface RemoteAdmin {
  /** The address on which to start the secure listener. Default: :2021 */
  listen?: string;
  /** List of access controls for this secure admin endpoint. This configures TLS mutual authentication (i.e. authorized client certificates), but also application-layer permissions like which paths and methods each identity is authorized for. AdminAccess specifies what permissions an identity or group of identities are granted. */
  access_control?: RemoteAdminAccess_control;
}
export const RemoteAdmin = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    listen: S.optional(S.String),
    access_control: S.optional(RemoteAdminAccess_control),
  }),
).annotate({ identifier: "RemoteAdmin" }) as any as S.Schema<RemoteAdmin>;

/** AdminConfig configures Caddy's API endpoint, which is used to manage Caddy while it is running. */
export interface AdminConfig {
  /** If true, the admin endpoint will be completely disabled. Note that this makes any runtime changes to the config impossible, since the interface to do so is through the admin endpoint. */
  disabled?: boolean;
  /** The address to which the admin endpoint's listener should bind itself. Can be any single network address that can be parsed by Caddy. Default: localhost:2019 */
  listen?: string;
  /** If true, CORS headers will be emitted, and requests to the API will be rejected if their `Host` and `Origin` headers do not match the expected value(s). Use `origins` to customize which origins/hosts are allowed. If `origins` is not set, the listen address is the only value allowed by default. Enforced only on local (plaintext) endpoint. */
  enforce_origin?: boolean;
  /** The list of allowed origins/hosts for API requests. Only needed if accessing the admin endpoint from a host different from the socket's network interface or if `enforce_origin` is true. If not set, the listener address will be the default value. If set but empty, no origins will be allowed. Enforced only on local (plaintext) endpoint. */
  origins?: AdminConfigOrigins;
  /** Options pertaining to configuration management. ConfigSettings configures the management of configuration. */
  config?: ConfigSettings;
  /** Options that establish this server's identity. Identity refers to credentials which can be used to uniquely identify and authenticate this server instance. This is required if remote administration is enabled (but does not require remote administration to be enabled). Default: no identity management. IdentityConfig configures management of this server's identity. An identity consists of credentials that uniquely verify this instance; for example, TLS certificates (public + private key pairs). */
  identity?: IdentityConfig;
  /** Options pertaining to remote administration. By default, remote administration is disabled. If enabled, identity management must also be configured, as that is how the endpoint is secured. See the neighboring "identity" object. EXPERIMENTAL: This feature is subject to change. RemoteAdmin enables and configures remote administration. If enabled, a secure listener enforcing mutual TLS authentication will be started on a different port from the standard plaintext admin server. This endpoint is secured using identity management, which must be configured separately (because identity management does not depend on remote administration). See the admin/identity config struct. EXPERIMENTAL: Subject to change. */
  remote?: RemoteAdmin;
}
export const AdminConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    disabled: S.optional(S.Boolean),
    listen: S.optional(S.String),
    enforce_origin: S.optional(S.Boolean),
    origins: S.optional(AdminConfigOrigins),
    config: S.optional(ConfigSettings),
    identity: S.optional(IdentityConfig),
    remote: S.optional(RemoteAdmin),
  }),
).annotate({ identifier: "AdminConfig" }) as any as S.Schema<AdminConfig>;

/** Sink is the destination for all unstructured logs emitted from Go's standard library logger. These logs are common in dependencies that are not designed specifically for use in Caddy. Because it is global and unstructured, the sink lacks most advanced features and customizations. StandardLibLog configures the default Go standard library global logger in the log package. This is necessary because module dependencies which are not built specifically for Caddy will use the standard logger. This is also known as the "sink" logger. */
export interface StandardLibLog {
  /** The module that writes out log entries for the sink. Module — the name is one of com.caddy.admin#CaddyLoggingWritersModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  writer?: unknown;
}
export const StandardLibLog = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    writer: S.optional(S.Unknown),
  }),
).annotate({ identifier: "StandardLibLog" }) as any as S.Schema<StandardLibLog>;

/** Sampling configures log entry sampling. If enabled, only some log entries will be emitted. This is useful for improving performance on extremely high-pressure servers. LogSampling configures log entry sampling. */
export interface LogSampling {
  /** The window over which to conduct sampling. A Duration represents the elapsed time between two instants as an int64 nanosecond count. The representation limits the largest representable duration to approximately 290 years. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  interval?: unknown;
  /** Log this many entries within a given level and message for each interval. */
  first?: number;
  /** If more entries with the same level and message are seen during the same interval, keep one in this many entries until the end of the interval. */
  thereafter?: number;
}
export const LogSampling = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    interval: S.optional(S.Unknown),
    first: S.optional(S.Number),
    thereafter: S.optional(S.Number),
  }),
).annotate({ identifier: "LogSampling" }) as any as S.Schema<LogSampling>;

/** Include defines the names of loggers to emit in this log. For example, to include only logs emitted by the admin API, you would include "admin.api". */
export type CustomLogInclude = Array<string>;
export const CustomLogInclude = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CustomLogInclude>;

/** Exclude defines the names of loggers that should be skipped by this log. For example, to exclude only HTTP access logs, you would exclude "http.log.access". */
export type CustomLogExclude = Array<string>;
export const CustomLogExclude = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CustomLogExclude>;

/** Logs are your logs, keyed by an arbitrary name of your choosing. The default log can be customized by defining a log called "default". You can further define other logs and filter what kinds of entries they accept. CustomLog represents a custom logger configuration. By default, a log will emit all log entries. Some entries will be skipped if sampling is enabled. Further, the Include and Exclude parameters define which loggers (by name) are allowed or rejected from emitting in this log. If both Include and Exclude are populated, their values must be mutually exclusive, and longer namespaces have priority. If neither are populated, all logs are emitted. */
export interface CustomLog {
  /** The writer defines where log entries are emitted. Module — the name is one of com.caddy.admin#CaddyLoggingWritersModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  writer?: unknown;
  /** The encoder is how the log entries are formatted or encoded. Module — the name is one of com.caddy.admin#CaddyLoggingEncodersModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  encoder?: unknown;
  /** Level is the minimum level to emit, and is inclusive. Possible levels: DEBUG, INFO, WARN, ERROR, PANIC, and FATAL */
  level?: string;
  /** Sampling configures log entry sampling. If enabled, only some log entries will be emitted. This is useful for improving performance on extremely high-pressure servers. LogSampling configures log entry sampling. */
  sampling?: LogSampling;
  /** Include defines the names of loggers to emit in this log. For example, to include only logs emitted by the admin API, you would include "admin.api". */
  include?: CustomLogInclude;
  /** Exclude defines the names of loggers that should be skipped by this log. For example, to exclude only HTTP access logs, you would exclude "http.log.access". */
  exclude?: CustomLogExclude;
}
export const CustomLog = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    writer: S.optional(S.Unknown),
    encoder: S.optional(S.Unknown),
    level: S.optional(S.String),
    sampling: S.optional(LogSampling),
    include: S.optional(CustomLogInclude),
    exclude: S.optional(CustomLogExclude),
  }),
).annotate({ identifier: "CustomLog" }) as any as S.Schema<CustomLog>;

/** Logs are your logs, keyed by an arbitrary name of your choosing. The default log can be customized by defining a log called "default". You can further define other logs and filter what kinds of entries they accept. CustomLog represents a custom logger configuration. By default, a log will emit all log entries. Some entries will be skipped if sampling is enabled. Further, the Include and Exclude parameters define which loggers (by name) are allowed or rejected from emitting in this log. If both Include and Exclude are populated, their values must be mutually exclusive, and longer namespaces have priority. If neither are populated, all logs are emitted. */
export type LoggingLogs = { [key: string]: CustomLog | undefined };
export const LoggingLogs = /*@__PURE__*/ S.Record(
  S.String,
  CustomLog,
) as any as S.Schema<LoggingLogs>;

/** Logging facilitates logging within Caddy. The default log is called "default" and you can customize it. You can also define additional logs. By default, all logs at INFO level and higher are written to standard error ("stderr" writer) in a human-readable format ("console" encoder if stdout is an interactive terminal, "json" encoder otherwise). All defined logs accept all log entries by default, but you can filter by level and module/logger names. A logger's name is the same as the module's name, but a module may append to logger names for more specificity. For example, you can filter logs emitted only by HTTP handlers using the name "http.handlers", because all HTTP handler module names have that prefix. Caddy logs (except the sink) are zero-allocation, so they are very high-performing in terms of memory and CPU time. Enabling sampling can further increase throughput on extremely high-load servers. */
export interface Logging {
  /** Sink is the destination for all unstructured logs emitted from Go's standard library logger. These logs are common in dependencies that are not designed specifically for use in Caddy. Because it is global and unstructured, the sink lacks most advanced features and customizations. StandardLibLog configures the default Go standard library global logger in the log package. This is necessary because module dependencies which are not built specifically for Caddy will use the standard logger. This is also known as the "sink" logger. */
  sink?: StandardLibLog;
  /** Logs are your logs, keyed by an arbitrary name of your choosing. The default log can be customized by defining a log called "default". You can further define other logs and filter what kinds of entries they accept. CustomLog represents a custom logger configuration. By default, a log will emit all log entries. Some entries will be skipped if sampling is enabled. Further, the Include and Exclude parameters define which loggers (by name) are allowed or rejected from emitting in this log. If both Include and Exclude are populated, their values must be mutually exclusive, and longer namespaces have priority. If neither are populated, all logs are emitted. */
  logs?: LoggingLogs;
}
export const Logging = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sink: S.optional(StandardLibLog),
    logs: S.optional(LoggingLogs),
  }),
).annotate({ identifier: "Logging" }) as any as S.Schema<Logging>;

/** Socket addresses to which to bind listeners. Accepts [network addresses](/docs/conventions#network-addresses) that may include port ranges. Listener addresses must be unique; they cannot be repeated across all defined servers. */
export type ServerListen = Array<string>;
export const ServerListen = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ServerListen>;

/** A list of listener wrapper modules, which can modify the behavior of the base listener. They are applied in the given order. */
export type ServerListener_wrappers = Array<unknown>;
export const ServerListener_wrappers = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<ServerListener_wrappers>;

/** The matcher sets which will be used to qualify this route for a request (essentially the "if" statement of this route). Each matcher set is OR'ed, but matchers within a set are AND'ed together. ModuleMap is a map that can contain multiple modules, where the map key is the module's name. (The namespace is usually read from an associated field's struct tag.) Because the module's name is given as the key in a module map, the name does not have to be given in the json.RawMessage. */
export type RouteMatch = Array<unknown>;
export const RouteMatch = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<RouteMatch>;

/** The list of handlers for this route. Upon matching a request, they are chained together in a middleware fashion: requests flow from the first handler to the last (top of the list to the bottom), with the possibility that any handler could stop the chain and/or return an error. Responses flow back through the chain (bottom of the list to the top) as they are written out to the client. Not all handlers call the next handler in the chain. For example, the reverse_proxy handler always sends a request upstream or returns an error. Thus, configuring handlers after reverse_proxy in the same route is illogical, since they would never be executed. You will want to put handlers which originate the response at the very end of your route(s). The documentation for a module should state whether it invokes the next handler, but sometimes it is common sense. Some handlers manipulate the response. Remember that requests flow down the list, and responses flow up the list. For example, if you wanted to use both `templates` and `encode` handlers, you would need to put `templates` after `encode` in your route, because responses flow up. Thus, `templates` will be able to parse and execute the plain-text response as a template, and then return it up to the `encode` handler which will then compress it into a binary format. If `templates` came before `encode`, then `encode` would write a compressed, binary-encoded response to `templates` which would not be able to parse the response properly. The correct order, then, is this: [ {"handler": "encode"}, {"handler": "templates"}, {"handler": "file_server"} ] The request flows ⬇️ DOWN (`encode` -> `templates` -> `file_server`). 1. First, `encode` will choose how to `encode` the response and wrap the response. 2. Then, `templates` will wrap the response with a buffer. 3. Finally, `file_server` will originate the content from a file. The response flows ⬆️ UP (`file_server` -> `templates` -> `encode`): 1. First, `file_server` will write the file to the response. 2. That write will be buffered and then executed by `templates`. 3. Lastly, the write from `templates` will flow into `encode` which will compress the stream. If you think of routes in this way, it will be easy and even fun to solve the puzzle of writing correct routes. */
export type RouteHandle = Array<unknown>;
export const RouteHandle = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<RouteHandle>;

/** Routes describes how this server will handle requests. Routes are executed sequentially. First a route's matchers are evaluated, then its grouping. If it matches and has not been mutually-excluded by its grouping, then its handlers are executed sequentially. The sequence of invoked handlers comprises a compiled middleware chain that flows from each matching route and its handlers to the next. By default, all unrouted requests receive a 200 OK response to indicate the server is working. Route consists of a set of rules for matching HTTP requests, a list of handlers to execute, and optional flow control parameters which customize the handling of HTTP requests in a highly flexible and performant manner. */
export interface Route {
  /** Group is an optional name for a group to which this route belongs. Grouping a route makes it mutually exclusive with others in its group; if a route belongs to a group, only the first matching route in that group will be executed. */
  group?: string;
  /** The matcher sets which will be used to qualify this route for a request (essentially the "if" statement of this route). Each matcher set is OR'ed, but matchers within a set are AND'ed together. ModuleMap is a map that can contain multiple modules, where the map key is the module's name. (The namespace is usually read from an associated field's struct tag.) Because the module's name is given as the key in a module map, the name does not have to be given in the json.RawMessage. */
  match?: RouteMatch;
  /** The list of handlers for this route. Upon matching a request, they are chained together in a middleware fashion: requests flow from the first handler to the last (top of the list to the bottom), with the possibility that any handler could stop the chain and/or return an error. Responses flow back through the chain (bottom of the list to the top) as they are written out to the client. Not all handlers call the next handler in the chain. For example, the reverse_proxy handler always sends a request upstream or returns an error. Thus, configuring handlers after reverse_proxy in the same route is illogical, since they would never be executed. You will want to put handlers which originate the response at the very end of your route(s). The documentation for a module should state whether it invokes the next handler, but sometimes it is common sense. Some handlers manipulate the response. Remember that requests flow down the list, and responses flow up the list. For example, if you wanted to use both `templates` and `encode` handlers, you would need to put `templates` after `encode` in your route, because responses flow up. Thus, `templates` will be able to parse and execute the plain-text response as a template, and then return it up to the `encode` handler which will then compress it into a binary format. If `templates` came before `encode`, then `encode` would write a compressed, binary-encoded response to `templates` which would not be able to parse the response properly. The correct order, then, is this: [ {"handler": "encode"}, {"handler": "templates"}, {"handler": "file_server"} ] The request flows ⬇️ DOWN (`encode` -> `templates` -> `file_server`). 1. First, `encode` will choose how to `encode` the response and wrap the response. 2. Then, `templates` will wrap the response with a buffer. 3. Finally, `file_server` will originate the content from a file. The response flows ⬆️ UP (`file_server` -> `templates` -> `encode`): 1. First, `file_server` will write the file to the response. 2. That write will be buffered and then executed by `templates`. 3. Lastly, the write from `templates` will flow into `encode` which will compress the stream. If you think of routes in this way, it will be easy and even fun to solve the puzzle of writing correct routes. */
  handle?: RouteHandle;
  /** If true, no more routes will be executed after this one. */
  terminal?: boolean;
}
export const Route = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    group: S.optional(S.String),
    match: S.optional(RouteMatch),
    handle: S.optional(RouteHandle),
    terminal: S.optional(S.Boolean),
  }),
).annotate({ identifier: "Route" }) as any as S.Schema<Route>;

/** Routes describes how this server will handle requests. Routes are executed sequentially. First a route's matchers are evaluated, then its grouping. If it matches and has not been mutually-excluded by its grouping, then its handlers are executed sequentially. The sequence of invoked handlers comprises a compiled middleware chain that flows from each matching route and its handlers to the next. By default, all unrouted requests receive a 200 OK response to indicate the server is working. Route consists of a set of rules for matching HTTP requests, a list of handlers to execute, and optional flow control parameters which customize the handling of HTTP requests in a highly flexible and performant manner. */
export type ServerRoutes = Array<Route>;
export const ServerRoutes = /*@__PURE__*/ S.Array(
  Route,
) as any as S.Schema<ServerRoutes>;

/** The routes to evaluate after the primary handler chain returns an error. In an error route, extra placeholders are available: Placeholder | Description ------------|--------------- `{http.error.status_code}` | The recommended HTTP status code `{http.error.status_text}` | The status text associated with the recommended status code `{http.error.message}` | The error message `{http.error.trace}` | The origin of the error `{http.error.id}` | An identifier for this occurrence of the error Route consists of a set of rules for matching HTTP requests, a list of handlers to execute, and optional flow control parameters which customize the handling of HTTP requests in a highly flexible and performant manner. */
export type HTTPErrorConfigRoutes = Array<Route>;
export const HTTPErrorConfigRoutes = /*@__PURE__*/ S.Array(
  Route,
) as any as S.Schema<HTTPErrorConfigRoutes>;

/** Errors is how this server will handle errors returned from any of the handlers in the primary routes. If the primary handler chain returns an error, the error along with its recommended status code are bubbled back up to the HTTP server which executes a separate error route, specified using this property. The error routes work exactly like the normal routes. HTTPErrorConfig determines how to handle errors from the HTTP handlers. */
export interface HTTPErrorConfig {
  /** The routes to evaluate after the primary handler chain returns an error. In an error route, extra placeholders are available: Placeholder | Description ------------|--------------- `{http.error.status_code}` | The recommended HTTP status code `{http.error.status_text}` | The status text associated with the recommended status code `{http.error.message}` | The error message `{http.error.trace}` | The origin of the error `{http.error.id}` | An identifier for this occurrence of the error Route consists of a set of rules for matching HTTP requests, a list of handlers to execute, and optional flow control parameters which customize the handling of HTTP requests in a highly flexible and performant manner. */
  routes?: HTTPErrorConfigRoutes;
}
export const HTTPErrorConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    routes: S.optional(HTTPErrorConfigRoutes),
  }),
).annotate({
  identifier: "HTTPErrorConfig",
}) as any as S.Schema<HTTPErrorConfig>;

/** NamedRoutes describes a mapping of reusable routes that can be invoked by their name. This can be used to optimize memory usage when the same route is needed for many subroutes, by having the handlers and matchers be only provisioned once, but used from many places. These routes are not executed unless they are invoked from another route. EXPERIMENTAL: Subject to change or removal. Route consists of a set of rules for matching HTTP requests, a list of handlers to execute, and optional flow control parameters which customize the handling of HTTP requests in a highly flexible and performant manner. */
export type ServerNamed_routes = { [key: string]: Route | undefined };
export const ServerNamed_routes = /*@__PURE__*/ S.Record(
  S.String,
  Route,
) as any as S.Schema<ServerNamed_routes>;

/** The certificate must have one of these serial numbers. bigInt is a big.Int type that interops with JSON encodings as a string. */
export interface bigInt {}
export const bigInt = /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
  identifier: "bigInt",
}) as any as S.Schema<bigInt>;

/** The certificate must have one of these serial numbers. bigInt is a big.Int type that interops with JSON encodings as a string. */
export type CustomCertSelectionPolicySerial_number = Array<bigInt>;
export const CustomCertSelectionPolicySerial_number = /*@__PURE__*/ S.Array(
  bigInt,
) as any as S.Schema<CustomCertSelectionPolicySerial_number>;

/** The certificate must have one of these organization names. */
export type CustomCertSelectionPolicySubject_organization = Array<string>;
export const CustomCertSelectionPolicySubject_organization =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CustomCertSelectionPolicySubject_organization>;

/** The certificate must have at least one of the tags in the list. */
export type CustomCertSelectionPolicyAny_tag = Array<string>;
export const CustomCertSelectionPolicyAny_tag = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CustomCertSelectionPolicyAny_tag>;

/** The certificate must have all of the tags in the list. */
export type CustomCertSelectionPolicyAll_tags = Array<string>;
export const CustomCertSelectionPolicyAll_tags = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CustomCertSelectionPolicyAll_tags>;

/** How to choose a certificate if more than one matched the given ServerName (SNI) value. CustomCertSelectionPolicy represents a policy for selecting the certificate used to complete a handshake when there may be multiple options. All fields specified must match the candidate certificate for it to be chosen. This was needed to solve https://github.com/caddyserver/caddy/issues/2588. */
export interface CustomCertSelectionPolicy {
  /** The certificate must have one of these serial numbers. bigInt is a big.Int type that interops with JSON encodings as a string. */
  serial_number?: CustomCertSelectionPolicySerial_number;
  /** The certificate must have one of these organization names. */
  subject_organization?: CustomCertSelectionPolicySubject_organization;
  /** The certificate must use this public key algorithm. PublicKeyAlgorithm is a JSON-unmarshalable wrapper type. */
  public_key_algorithm?: number;
  /** The certificate must have at least one of the tags in the list. */
  any_tag?: CustomCertSelectionPolicyAny_tag;
  /** The certificate must have all of the tags in the list. */
  all_tags?: CustomCertSelectionPolicyAll_tags;
}
export const CustomCertSelectionPolicy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    serial_number: S.optional(CustomCertSelectionPolicySerial_number),
    subject_organization: S.optional(
      CustomCertSelectionPolicySubject_organization,
    ),
    public_key_algorithm: S.optional(S.Number),
    any_tag: S.optional(CustomCertSelectionPolicyAny_tag),
    all_tags: S.optional(CustomCertSelectionPolicyAll_tags),
  }),
).annotate({
  identifier: "CustomCertSelectionPolicy",
}) as any as S.Schema<CustomCertSelectionPolicy>;

/** The list of cipher suites to support. Caddy's defaults are modern and secure. */
export type ConnectionPolicyCipher_suites = Array<string>;
export const ConnectionPolicyCipher_suites = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ConnectionPolicyCipher_suites>;

/** The list of elliptic curves to support. Caddy's defaults are modern and secure. */
export type ConnectionPolicyCurves = Array<string>;
export const ConnectionPolicyCurves = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ConnectionPolicyCurves>;

/** Protocols to use for Application-Layer Protocol Negotiation (ALPN) during the handshake. */
export type ConnectionPolicyAlpn = Array<string>;
export const ConnectionPolicyAlpn = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ConnectionPolicyAlpn>;

/** Deprecated: Use the `ca` field with the `tls.ca_pool.source.inline` module instead. A list of base64 DER-encoded CA certificates against which to validate client certificates. Client certs which are not signed by any of these CAs will be rejected. */
export type ClientAuthenticationTrusted_ca_certs = Array<string>;
export const ClientAuthenticationTrusted_ca_certs = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ClientAuthenticationTrusted_ca_certs>;

/** Deprecated: Use the `ca` field with the `tls.ca_pool.source.file` module instead. TrustedCACertPEMFiles is a list of PEM file names from which to load certificates of trusted CAs. Client certificates which are not signed by any of these CA certificates will be rejected. */
export type ClientAuthenticationTrusted_ca_certs_pem_files = Array<string>;
export const ClientAuthenticationTrusted_ca_certs_pem_files =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<ClientAuthenticationTrusted_ca_certs_pem_files>;

/** Deprecated: This field is deprecated and will be removed in a future version. Please use the `validators` field instead with the tls.client_auth.verifier.leaf module instead. A list of base64 DER-encoded client leaf certs to accept. If this list is not empty, client certs which are not in this list will be rejected. */
export type ClientAuthenticationTrusted_leaf_certs = Array<string>;
export const ClientAuthenticationTrusted_leaf_certs = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ClientAuthenticationTrusted_leaf_certs>;

/** Client certificate verification modules. These can perform custom client authentication checks, such as ensuring the certificate is not revoked. */
export type ClientAuthenticationVerifiers = Array<unknown>;
export const ClientAuthenticationVerifiers = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<ClientAuthenticationVerifiers>;

/** Enables and configures TLS client authentication. ClientAuthentication configures TLS client auth. */
export interface ClientAuthentication {
  /** Certificate authority module which provides the certificate pool of trusted certificates Module — the name is one of com.caddy.admin#TlsCaPoolSourceModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  ca?: unknown;
  /** Deprecated: Use the `ca` field with the `tls.ca_pool.source.inline` module instead. A list of base64 DER-encoded CA certificates against which to validate client certificates. Client certs which are not signed by any of these CAs will be rejected. */
  trusted_ca_certs?: ClientAuthenticationTrusted_ca_certs;
  /** Deprecated: Use the `ca` field with the `tls.ca_pool.source.file` module instead. TrustedCACertPEMFiles is a list of PEM file names from which to load certificates of trusted CAs. Client certificates which are not signed by any of these CA certificates will be rejected. */
  trusted_ca_certs_pem_files?: ClientAuthenticationTrusted_ca_certs_pem_files;
  /** Deprecated: This field is deprecated and will be removed in a future version. Please use the `validators` field instead with the tls.client_auth.verifier.leaf module instead. A list of base64 DER-encoded client leaf certs to accept. If this list is not empty, client certs which are not in this list will be rejected. */
  trusted_leaf_certs?: ClientAuthenticationTrusted_leaf_certs;
  /** Client certificate verification modules. These can perform custom client authentication checks, such as ensuring the certificate is not revoked. */
  verifiers?: ClientAuthenticationVerifiers;
  /** The mode for authenticating the client. Allowed values are: Mode | Description -----|--------------- `request` | Ask clients for a certificate, but allow even if there isn't one; do not verify it `require` | Require clients to present a certificate, but do not verify it `verify_if_given` | Ask clients for a certificate; allow even if there isn't one, but verify it if there is `require_and_verify` | Require clients to present a valid certificate that is verified The default mode is `require_and_verify` if any TrustedCACerts or TrustedCACertPEMFiles or TrustedLeafCerts are provided; otherwise, the default mode is `require`. */
  mode?: string;
}
export const ClientAuthentication = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ca: S.optional(S.Unknown),
    trusted_ca_certs: S.optional(ClientAuthenticationTrusted_ca_certs),
    trusted_ca_certs_pem_files: S.optional(
      ClientAuthenticationTrusted_ca_certs_pem_files,
    ),
    trusted_leaf_certs: S.optional(ClientAuthenticationTrusted_leaf_certs),
    verifiers: S.optional(ClientAuthenticationVerifiers),
    mode: S.optional(S.String),
  }),
).annotate({
  identifier: "ClientAuthentication",
}) as any as S.Schema<ClientAuthentication>;

/** How to handle TLS connections. At least one policy is required to enable HTTPS on this server if automatic HTTPS is disabled or does not apply. ConnectionPolicy specifies the logic for handling a TLS handshake. An empty policy is valid; safe and sensible defaults will be used. */
export interface ConnectionPolicy {
  /** How to match this policy with a TLS ClientHello. If this policy is the first to match, it will be used. ModuleMap is a map that can contain multiple modules, where the map key is the module's name. (The namespace is usually read from an associated field's struct tag.) Because the module's name is given as the key in a module map, the name does not have to be given in the json.RawMessage. Module map — the name is one of com.caddy.admin#TlsHandshakeMatchModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  match?: unknown;
  /** How to choose a certificate if more than one matched the given ServerName (SNI) value. CustomCertSelectionPolicy represents a policy for selecting the certificate used to complete a handshake when there may be multiple options. All fields specified must match the candidate certificate for it to be chosen. This was needed to solve https://github.com/caddyserver/caddy/issues/2588. */
  certificate_selection?: CustomCertSelectionPolicy;
  /** The list of cipher suites to support. Caddy's defaults are modern and secure. */
  cipher_suites?: ConnectionPolicyCipher_suites;
  /** The list of elliptic curves to support. Caddy's defaults are modern and secure. */
  curves?: ConnectionPolicyCurves;
  /** Protocols to use for Application-Layer Protocol Negotiation (ALPN) during the handshake. */
  alpn?: ConnectionPolicyAlpn;
  /** Minimum TLS protocol version to allow. Default: `tls1.2` */
  protocol_min?: string;
  /** Maximum TLS protocol version to allow. Default: `tls1.3` */
  protocol_max?: string;
  /** Reject TLS connections. EXPERIMENTAL: May change. */
  drop?: boolean;
  /** Enables and configures TLS client authentication. ClientAuthentication configures TLS client auth. */
  client_authentication?: ClientAuthentication;
  /** DefaultSNI becomes the ServerName in a ClientHello if there is no policy configured for the empty SNI value. */
  default_sni?: string;
  /** FallbackSNI becomes the ServerName in a ClientHello if the original ServerName doesn't match any certificates in the cache. The use cases for this are very niche; typically if a client is a CDN and passes through the ServerName of the downstream handshake but can accept a certificate with the origin's hostname instead, then you would set this to your origin's hostname. Note that Caddy must be managing a certificate for this name. This feature is EXPERIMENTAL and subject to change or removal. */
  fallback_sni?: string;
  /** Also known as "SSLKEYLOGFILE", TLS secrets will be written to this file in NSS key log format which can then be parsed by Wireshark and other tools. This is INSECURE as it allows other programs or tools to decrypt TLS connections. However, this capability can be useful for debugging and troubleshooting. **ENABLING THIS LOG COMPROMISES SECURITY!** This feature is EXPERIMENTAL and subject to change or removal. */
  insecure_secrets_log?: string;
  /** A module that can manipulate the context passed into CertMagic's certificate management functions during TLS handshakes. EXPERIMENTAL - subject to change or removal. Module — the name is one of com.caddy.admin#TlsContextModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  handshake_context?: unknown;
}
export const ConnectionPolicy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    match: S.optional(S.Unknown),
    certificate_selection: S.optional(CustomCertSelectionPolicy),
    cipher_suites: S.optional(ConnectionPolicyCipher_suites),
    curves: S.optional(ConnectionPolicyCurves),
    alpn: S.optional(ConnectionPolicyAlpn),
    protocol_min: S.optional(S.String),
    protocol_max: S.optional(S.String),
    drop: S.optional(S.Boolean),
    client_authentication: S.optional(ClientAuthentication),
    default_sni: S.optional(S.String),
    fallback_sni: S.optional(S.String),
    insecure_secrets_log: S.optional(S.String),
    handshake_context: S.optional(S.Unknown),
  }),
).annotate({
  identifier: "ConnectionPolicy",
}) as any as S.Schema<ConnectionPolicy>;

/** How to handle TLS connections. At least one policy is required to enable HTTPS on this server if automatic HTTPS is disabled or does not apply. ConnectionPolicy specifies the logic for handling a TLS handshake. An empty policy is valid; safe and sensible defaults will be used. */
export type ServerTls_connection_policies = Array<ConnectionPolicy>;
export const ServerTls_connection_policies = /*@__PURE__*/ S.Array(
  ConnectionPolicy,
) as any as S.Schema<ServerTls_connection_policies>;

/** Hosts/domain names listed here will not be included in automatic HTTPS (they will not have certificates loaded nor redirects applied). */
export type AutoHTTPSConfigSkip = Array<string>;
export const AutoHTTPSConfigSkip = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AutoHTTPSConfigSkip>;

/** Hosts/domain names listed here will still be enabled for automatic HTTPS (unless in the Skip list), except that certificates will not be provisioned and managed for these names. */
export type AutoHTTPSConfigSkip_certificates = Array<string>;
export const AutoHTTPSConfigSkip_certificates = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AutoHTTPSConfigSkip_certificates>;

/** AutoHTTPS configures or disables automatic HTTPS within this server. HTTPS is enabled automatically and by default when qualifying names are present in a Host matcher and/or when the server is listening only on the HTTPS port. AutoHTTPSConfig is used to disable automatic HTTPS or certain aspects of it for a specific server. HTTPS is enabled automatically and by default when qualifying hostnames are available from the config. */
export interface AutoHTTPSConfig {
  /** If true, automatic HTTPS will be entirely disabled, including certificate management and redirects. */
  disable?: boolean;
  /** If true, only automatic HTTP->HTTPS redirects will be disabled, but other auto-HTTPS features will remain enabled. */
  disable_redirects?: boolean;
  /** If true, automatic certificate management will be disabled, but other auto-HTTPS features will remain enabled. */
  disable_certificates?: boolean;
  /** Hosts/domain names listed here will not be included in automatic HTTPS (they will not have certificates loaded nor redirects applied). */
  skip?: AutoHTTPSConfigSkip;
  /** Hosts/domain names listed here will still be enabled for automatic HTTPS (unless in the Skip list), except that certificates will not be provisioned and managed for these names. */
  skip_certificates?: AutoHTTPSConfigSkip_certificates;
  /** By default, automatic HTTPS will obtain and renew certificates for qualifying hostnames. However, if a certificate with a matching SAN is already loaded into the cache, certificate management will not be enabled. To force automated certificate management regardless of loaded certificates, set this to true. */
  ignore_loaded_certificates?: boolean;
  /** If true, automatic HTTPS will prefer wildcard names and ignore non-wildcard names if both are available. This allows for writing a config with top-level host matchers without having those names produce certificates. */
  prefer_wildcard?: boolean;
}
export const AutoHTTPSConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    disable: S.optional(S.Boolean),
    disable_redirects: S.optional(S.Boolean),
    disable_certificates: S.optional(S.Boolean),
    skip: S.optional(AutoHTTPSConfigSkip),
    skip_certificates: S.optional(AutoHTTPSConfigSkip_certificates),
    ignore_loaded_certificates: S.optional(S.Boolean),
    prefer_wildcard: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "AutoHTTPSConfig",
}) as any as S.Schema<AutoHTTPSConfig>;

/** The headers from which the client IP address could be read from. These will be considered in order, with the first good value being used as the client IP. By default, only `X-Forwarded-For` is considered. This depends on `trusted_proxies` being configured and the request being validated as coming from a trusted proxy, otherwise the client IP will be set to the direct remote IP address. */
export type ServerClient_ip_headers = Array<string>;
export const ServerClient_ip_headers = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ServerClient_ip_headers>;

export type ServerLogConfigLogger_namesValue = Array<string>;
export const ServerLogConfigLogger_namesValue = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ServerLogConfigLogger_namesValue>;

/** LoggerNames maps request hostnames to one or more custom logger names. For example, a mapping of `"example.com": ["example"]` would cause access logs from requests with a Host of example.com to be emitted by a logger named "http.log.access.example". If there are multiple logger names, then the log will be emitted to all of them. If the logger name is an empty, the default logger is used, i.e. the logger "http.log.access". Keys must be hostnames (without ports), and may contain wildcards to match subdomains. The value is an array of logger names. For backwards compatibility, if the value is a string, it is treated as a single-element array. StringArray is a slices of strings, but also accepts a single string as a value when JSON unmarshaling, converting it to a slice of one string. */
export type ServerLogConfigLogger_names = {
  [key: string]: ServerLogConfigLogger_namesValue | undefined;
};
export const ServerLogConfigLogger_names = /*@__PURE__*/ S.Record(
  S.String,
  ServerLogConfigLogger_namesValue,
) as any as S.Schema<ServerLogConfigLogger_names>;

/** By default, all requests to this server will be logged if access logging is enabled. This field lists the request hosts for which access logging should be disabled. */
export type ServerLogConfigSkip_hosts = Array<string>;
export const ServerLogConfigSkip_hosts = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ServerLogConfigSkip_hosts>;

/** Enables access logging and configures how access logs are handled in this server. To minimally enable access logs, simply set this to a non-null, empty struct. ServerLogConfig describes a server's logging configuration. If enabled without customization, all requests to this server are logged to the default logger; logger destinations may be customized per-request-host. */
export interface ServerLogConfig {
  /** The default logger name for all logs emitted by this server for hostnames that are not in the logger_names map. */
  default_logger_name?: string;
  /** LoggerNames maps request hostnames to one or more custom logger names. For example, a mapping of `"example.com": ["example"]` would cause access logs from requests with a Host of example.com to be emitted by a logger named "http.log.access.example". If there are multiple logger names, then the log will be emitted to all of them. If the logger name is an empty, the default logger is used, i.e. the logger "http.log.access". Keys must be hostnames (without ports), and may contain wildcards to match subdomains. The value is an array of logger names. For backwards compatibility, if the value is a string, it is treated as a single-element array. StringArray is a slices of strings, but also accepts a single string as a value when JSON unmarshaling, converting it to a slice of one string. */
  logger_names?: ServerLogConfigLogger_names;
  /** By default, all requests to this server will be logged if access logging is enabled. This field lists the request hosts for which access logging should be disabled. */
  skip_hosts?: ServerLogConfigSkip_hosts;
  /** If true, requests to any host not appearing in the logger_names map will not be logged. */
  skip_unmapped_hosts?: boolean;
  /** If true, credentials that are otherwise omitted, will be logged. The definition of credentials is defined by https://fetch.spec.whatwg.org/#credentials, and this includes some request and response headers, i.e `Cookie`, `Set-Cookie`, `Authorization`, and `Proxy-Authorization`. */
  should_log_credentials?: boolean;
  /** Log each individual handler that is invoked. Requires that the log emit at DEBUG level. NOTE: This may log the configuration of your HTTP handler modules; do not enable this in insecure contexts when there is sensitive data in the configuration. EXPERIMENTAL: Subject to change or removal. */
  trace?: boolean;
}
export const ServerLogConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    default_logger_name: S.optional(S.String),
    logger_names: S.optional(ServerLogConfigLogger_names),
    skip_hosts: S.optional(ServerLogConfigSkip_hosts),
    skip_unmapped_hosts: S.optional(S.Boolean),
    should_log_credentials: S.optional(S.Boolean),
    trace: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "ServerLogConfig",
}) as any as S.Schema<ServerLogConfig>;

/** Protocols specifies which HTTP protocols to enable. Supported values are: - `h1` (HTTP/1.1) - `h2` (HTTP/2) - `h2c` (cleartext HTTP/2) - `h3` (HTTP/3) If enabling `h2` or `h2c`, `h1` must also be enabled; this is due to current limitations in the Go standard library. HTTP/2 operates only over TLS (HTTPS). HTTP/3 opens a UDP socket to serve QUIC connections. H2C operates over plain TCP if the client supports it; however, because this is not implemented by the Go standard library, other server options are not compatible and will not be applied to H2C requests. Do not enable this only to achieve maximum client compatibility. In practice, very few clients implement H2C, and even fewer require it. Enabling H2C can be useful for serving/proxying gRPC if encryption is not possible or desired. We recommend for most users to simply let Caddy use the default settings. Default: `[h1 h2 h3]` */
export type ServerProtocols = Array<string>;
export const ServerProtocols = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ServerProtocols>;

export type ServerListen_protocolsItem = Array<string>;
export const ServerListen_protocolsItem = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ServerListen_protocolsItem>;

/** ListenProtocols overrides Protocols for each parallel address in Listen. A nil value or element indicates that Protocols will be used instead. */
export type ServerListen_protocols = Array<ServerListen_protocolsItem>;
export const ServerListen_protocols = /*@__PURE__*/ S.Array(
  ServerListen_protocolsItem,
) as any as S.Schema<ServerListen_protocols>;

/** If set, metrics observations will be enabled. This setting is EXPERIMENTAL and subject to change. DEPRECATED: Use the app-level `metrics` field. Metrics configures metrics observations. EXPERIMENTAL and subject to change or removal. */
export interface Metrics {
  /** Enable per-host metrics. Enabling this option may incur high-memory consumption, depending on the number of hosts managed by Caddy. */
  per_host?: boolean;
}
export const Metrics = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    per_host: S.optional(S.Boolean),
  }),
).annotate({ identifier: "Metrics" }) as any as S.Schema<Metrics>;

/** Servers is the list of servers, keyed by arbitrary names chosen at your discretion for your own convenience; the keys do not affect functionality. Server describes an HTTP server. */
export interface Server {
  /** Socket addresses to which to bind listeners. Accepts [network addresses](/docs/conventions#network-addresses) that may include port ranges. Listener addresses must be unique; they cannot be repeated across all defined servers. */
  listen?: ServerListen;
  /** A list of listener wrapper modules, which can modify the behavior of the base listener. They are applied in the given order. */
  listener_wrappers?: ServerListener_wrappers;
  /** How long to allow a read from a client's upload. Setting this to a short, non-zero value can mitigate slowloris attacks, but may also affect legitimately slow clients. Duration can be an integer or a string. An integer is interpreted as nanoseconds. If a string, it is a Go time.Duration value such as `300ms`, `1.5h`, or `2h45m`; valid units are `ns`, `us`/`µs`, `ms`, `s`, `m`, `h`, and `d`. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  read_timeout?: unknown;
  /** ReadHeaderTimeout is like ReadTimeout but for request headers. Default is 1 minute. Duration can be an integer or a string. An integer is interpreted as nanoseconds. If a string, it is a Go time.Duration value such as `300ms`, `1.5h`, or `2h45m`; valid units are `ns`, `us`/`µs`, `ms`, `s`, `m`, `h`, and `d`. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  read_header_timeout?: unknown;
  /** WriteTimeout is how long to allow a write to a client. Note that setting this to a small value when serving large files may negatively affect legitimately slow clients. Duration can be an integer or a string. An integer is interpreted as nanoseconds. If a string, it is a Go time.Duration value such as `300ms`, `1.5h`, or `2h45m`; valid units are `ns`, `us`/`µs`, `ms`, `s`, `m`, `h`, and `d`. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  write_timeout?: unknown;
  /** IdleTimeout is the maximum time to wait for the next request when keep-alives are enabled. If zero, a default timeout of 5m is applied to help avoid resource exhaustion. Duration can be an integer or a string. An integer is interpreted as nanoseconds. If a string, it is a Go time.Duration value such as `300ms`, `1.5h`, or `2h45m`; valid units are `ns`, `us`/`µs`, `ms`, `s`, `m`, `h`, and `d`. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  idle_timeout?: unknown;
  /** KeepAliveInterval is the interval at which TCP keepalive packets are sent to keep the connection alive at the TCP layer when no other data is being transmitted. The default is 15s. Duration can be an integer or a string. An integer is interpreted as nanoseconds. If a string, it is a Go time.Duration value such as `300ms`, `1.5h`, or `2h45m`; valid units are `ns`, `us`/`µs`, `ms`, `s`, `m`, `h`, and `d`. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  keepalive_interval?: unknown;
  /** MaxHeaderBytes is the maximum size to parse from a client's HTTP request headers. */
  max_header_bytes?: number;
  /** Enable full-duplex communication for HTTP/1 requests. Only has an effect if Caddy was built with Go 1.21 or later. For HTTP/1 requests, the Go HTTP server by default consumes any unread portion of the request body before beginning to write the response, preventing handlers from concurrently reading from the request and writing the response. Enabling this option disables this behavior and permits handlers to continue to read from the request while concurrently writing the response. For HTTP/2 requests, the Go HTTP server always permits concurrent reads and responses, so this option has no effect. Test thoroughly with your HTTP clients, as some older clients may not support full-duplex HTTP/1 which can cause them to deadlock. See https://github.com/golang/go/issues/57786 for more info. TODO: This is an EXPERIMENTAL feature. Subject to change or removal. */
  enable_full_duplex?: boolean;
  /** Routes describes how this server will handle requests. Routes are executed sequentially. First a route's matchers are evaluated, then its grouping. If it matches and has not been mutually-excluded by its grouping, then its handlers are executed sequentially. The sequence of invoked handlers comprises a compiled middleware chain that flows from each matching route and its handlers to the next. By default, all unrouted requests receive a 200 OK response to indicate the server is working. Route consists of a set of rules for matching HTTP requests, a list of handlers to execute, and optional flow control parameters which customize the handling of HTTP requests in a highly flexible and performant manner. */
  routes?: ServerRoutes;
  /** Errors is how this server will handle errors returned from any of the handlers in the primary routes. If the primary handler chain returns an error, the error along with its recommended status code are bubbled back up to the HTTP server which executes a separate error route, specified using this property. The error routes work exactly like the normal routes. HTTPErrorConfig determines how to handle errors from the HTTP handlers. */
  errors?: HTTPErrorConfig;
  /** NamedRoutes describes a mapping of reusable routes that can be invoked by their name. This can be used to optimize memory usage when the same route is needed for many subroutes, by having the handlers and matchers be only provisioned once, but used from many places. These routes are not executed unless they are invoked from another route. EXPERIMENTAL: Subject to change or removal. Route consists of a set of rules for matching HTTP requests, a list of handlers to execute, and optional flow control parameters which customize the handling of HTTP requests in a highly flexible and performant manner. */
  named_routes?: ServerNamed_routes;
  /** How to handle TLS connections. At least one policy is required to enable HTTPS on this server if automatic HTTPS is disabled or does not apply. ConnectionPolicy specifies the logic for handling a TLS handshake. An empty policy is valid; safe and sensible defaults will be used. */
  tls_connection_policies?: ServerTls_connection_policies;
  /** AutoHTTPS configures or disables automatic HTTPS within this server. HTTPS is enabled automatically and by default when qualifying names are present in a Host matcher and/or when the server is listening only on the HTTPS port. AutoHTTPSConfig is used to disable automatic HTTPS or certain aspects of it for a specific server. HTTPS is enabled automatically and by default when qualifying hostnames are available from the config. */
  automatic_https?: AutoHTTPSConfig;
  /** If true, will require that a request's Host header match the value of the ServerName sent by the client's TLS ClientHello; often a necessary safeguard when using TLS client authentication. */
  strict_sni_host?: boolean;
  /** A module which provides a source of IP ranges, from which requests should be trusted. By default, no proxies are trusted. On its own, this configuration will not do anything, but it can be used as a default set of ranges for handlers or matchers in routes to pick up, instead of needing to configure each of them. See the `reverse_proxy` handler for example, which uses this to trust sensitive incoming `X-Forwarded-*` headers. Module — the name is one of com.caddy.admin#HttpIpSourcesModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  trusted_proxies?: unknown;
  /** The headers from which the client IP address could be read from. These will be considered in order, with the first good value being used as the client IP. By default, only `X-Forwarded-For` is considered. This depends on `trusted_proxies` being configured and the request being validated as coming from a trusted proxy, otherwise the client IP will be set to the direct remote IP address. */
  client_ip_headers?: ServerClient_ip_headers;
  /** If greater than zero, enables strict ClientIPHeaders (default X-Forwarded-For) parsing. If enabled, the ClientIPHeaders will be parsed from right to left, and the first value that is both valid and doesn't match the trusted proxy list will be used as client IP. If zero, the ClientIPHeaders will be parsed from left to right, and the first value that is a valid IP address will be used as client IP. This depends on `trusted_proxies` being configured. This option is disabled by default. */
  trusted_proxies_strict?: number;
  /** Enables access logging and configures how access logs are handled in this server. To minimally enable access logs, simply set this to a non-null, empty struct. ServerLogConfig describes a server's logging configuration. If enabled without customization, all requests to this server are logged to the default logger; logger destinations may be customized per-request-host. */
  logs?: ServerLogConfig;
  /** Protocols specifies which HTTP protocols to enable. Supported values are: - `h1` (HTTP/1.1) - `h2` (HTTP/2) - `h2c` (cleartext HTTP/2) - `h3` (HTTP/3) If enabling `h2` or `h2c`, `h1` must also be enabled; this is due to current limitations in the Go standard library. HTTP/2 operates only over TLS (HTTPS). HTTP/3 opens a UDP socket to serve QUIC connections. H2C operates over plain TCP if the client supports it; however, because this is not implemented by the Go standard library, other server options are not compatible and will not be applied to H2C requests. Do not enable this only to achieve maximum client compatibility. In practice, very few clients implement H2C, and even fewer require it. Enabling H2C can be useful for serving/proxying gRPC if encryption is not possible or desired. We recommend for most users to simply let Caddy use the default settings. Default: `[h1 h2 h3]` */
  protocols?: ServerProtocols;
  /** ListenProtocols overrides Protocols for each parallel address in Listen. A nil value or element indicates that Protocols will be used instead. */
  listen_protocols?: ServerListen_protocols;
  /** If set, metrics observations will be enabled. This setting is EXPERIMENTAL and subject to change. DEPRECATED: Use the app-level `metrics` field. Metrics configures metrics observations. EXPERIMENTAL and subject to change or removal. */
  metrics?: Metrics;
}
export const Server = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    listen: S.optional(ServerListen),
    listener_wrappers: S.optional(ServerListener_wrappers),
    read_timeout: S.optional(S.Unknown),
    read_header_timeout: S.optional(S.Unknown),
    write_timeout: S.optional(S.Unknown),
    idle_timeout: S.optional(S.Unknown),
    keepalive_interval: S.optional(S.Unknown),
    max_header_bytes: S.optional(S.Number),
    enable_full_duplex: S.optional(S.Boolean),
    routes: S.optional(ServerRoutes),
    errors: S.optional(HTTPErrorConfig),
    named_routes: S.optional(ServerNamed_routes),
    tls_connection_policies: S.optional(ServerTls_connection_policies),
    automatic_https: S.optional(AutoHTTPSConfig),
    strict_sni_host: S.optional(S.Boolean),
    trusted_proxies: S.optional(S.Unknown),
    client_ip_headers: S.optional(ServerClient_ip_headers),
    trusted_proxies_strict: S.optional(S.Number),
    logs: S.optional(ServerLogConfig),
    protocols: S.optional(ServerProtocols),
    listen_protocols: S.optional(ServerListen_protocols),
    metrics: S.optional(Metrics),
  }),
).annotate({ identifier: "Server" }) as any as S.Schema<Server>;

/** Servers is the list of servers, keyed by arbitrary names chosen at your discretion for your own convenience; the keys do not affect functionality. Server describes an HTTP server. */
export type AppServers = { [key: string]: Server | undefined };
export const AppServers = /*@__PURE__*/ S.Record(
  S.String,
  Server,
) as any as S.Schema<AppServers>;

/** App is a robust, production-ready HTTP server. HTTPS is enabled by default if host matchers with qualifying names are used in any of routes; certificates are automatically provisioned and renewed. Additionally, automatic HTTPS will also enable HTTPS for servers that listen only on the HTTPS port but which do not have any TLS connection policies defined by adding a good, default TLS connection policy. In HTTP routes, additional placeholders are available (replace any `*`): Placeholder | Description ------------|--------------- `{http.request.body}` | The request body (⚠️ inefficient; use only for debugging) `{http.request.cookie.*}` | HTTP request cookie `{http.request.duration}` | Time up to now spent handling the request (after decoding headers from client) `{http.request.duration_ms}` | Same as 'duration', but in milliseconds. `{http.request.uuid}` | The request unique identifier `{http.request.header.*}` | Specific request header field `{http.request.host}` | The host part of the request's Host header `{http.request.host.labels.*}` | Request host labels (0-based from right); e.g. for foo.example.com: 0=com, 1=example, 2=foo `{http.request.hostport}` | The host and port from the request's Host header `{http.request.method}` | The request method `{http.request.orig_method}` | The request's original method `{http.request.orig_uri}` | The request's original URI `{http.request.orig_uri.path}` | The request's original path `{http.request.orig_uri.path.*}` | Parts of the original path, split by `/` (0-based from left) `{http.request.orig_uri.path.dir}` | The request's original directory `{http.request.orig_uri.path.file}` | The request's original filename `{http.request.orig_uri.query}` | The request's original query string (without `?`) `{http.request.port}` | The port part of the request's Host header `{http.request.proto}` | The protocol of the request `{http.request.local.host}` | The host (IP) part of the local address the connection arrived on `{http.request.local.port}` | The port part of the local address the connection arrived on `{http.request.local}` | The local address the connection arrived on `{http.request.remote.host}` | The host (IP) part of the remote client's address `{http.request.remote.port}` | The port part of the remote client's address `{http.request.remote}` | The address of the remote client `{http.request.scheme}` | The request scheme, typically `http` or `https` `{http.request.tls.version}` | The TLS version name `{http.request.tls.cipher_suite}` | The TLS cipher suite `{http.request.tls.resumed}` | The TLS connection resumed a previous connection `{http.request.tls.proto}` | The negotiated next protocol `{http.request.tls.proto_mutual}` | The negotiated next protocol was advertised by the server `{http.request.tls.server_name}` | The server name requested by the client, if any `{http.request.tls.client.fingerprint}` | The SHA256 checksum of the client certificate `{http.request.tls.client.public_key}` | The public key of the client certificate. `{http.request.tls.client.public_key_sha256}` | The SHA256 checksum of the client's public key. `{http.request.tls.client.certificate_pem}` | The PEM-encoded value of the certificate. `{http.request.tls.client.certificate_der_base64}` | The base64-encoded value of the certificate. `{http.request.tls.client.issuer}` | The issuer DN of the client certificate `{http.request.tls.client.serial}` | The serial number of the client certificate `{http.request.tls.client.subject}` | The subject DN of the client certificate `{http.request.tls.client.san.dns_names.*}` | SAN DNS names(index optional) `{http.request.tls.client.san.emails.*}` | SAN email addresses (index optional) `{http.request.tls.client.san.ips.*}` | SAN IP addresses (index optional) `{http.request.tls.client.san.uris.*}` | SAN URIs (index optional) `{http.request.uri}` | The full request URI `{http.request.uri.path}` | The path component of the request URI `{http.request.uri.path.*}` | Parts of the path, split by `/` (0-based from left) `{http.request.uri.path.dir}` | The directory, excluding leaf filename `{http.request.uri.path.file}` | The filename of the path, excluding directory `{http.request.uri.query}` | The query string (without `?`) `{http.request.uri.query.*}` | Individual query string value `{http.response.header.*}` | Specific response header field `{http.vars.*}` | Custom variables in the HTTP handler chain `{http.shutting_down}` | True if the HTTP app is shutting down `{http.time_until_shutdown}` | Time until HTTP server shutdown, if scheduled */
export interface App {
  /** HTTPPort specifies the port to use for HTTP (as opposed to HTTPS), which is used when setting up HTTP->HTTPS redirects or ACME HTTP challenge solvers. Default: 80. */
  http_port?: number;
  /** HTTPSPort specifies the port to use for HTTPS, which is used when solving the ACME TLS-ALPN challenges, or whenever HTTPS is needed but no specific port number is given. Default: 443. */
  https_port?: number;
  /** GracePeriod is how long to wait for active connections when shutting down the servers. During the grace period, no new connections are accepted, idle connections are closed, and active connections will be given the full length of time to become idle and close. Once the grace period is over, connections will be forcefully closed. If zero, the grace period is eternal. Default: 0. Duration can be an integer or a string. An integer is interpreted as nanoseconds. If a string, it is a Go time.Duration value such as `300ms`, `1.5h`, or `2h45m`; valid units are `ns`, `us`/`µs`, `ms`, `s`, `m`, `h`, and `d`. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  grace_period?: unknown;
  /** ShutdownDelay is how long to wait before initiating the grace period. When this app is stopping (e.g. during a config reload or process exit), all servers will be shut down. Normally this immediately initiates the grace period. However, if this delay is configured, servers will not be shut down until the delay is over. During this time, servers continue to function normally and allow new connections. At the end, the grace period will begin. This can be useful to allow downstream load balancers time to move this instance out of the rotation without hiccups. When shutdown has been scheduled, placeholders {http.shutting_down} (bool) and {http.time_until_shutdown} (duration) may be useful for health checks. Duration can be an integer or a string. An integer is interpreted as nanoseconds. If a string, it is a Go time.Duration value such as `300ms`, `1.5h`, or `2h45m`; valid units are `ns`, `us`/`µs`, `ms`, `s`, `m`, `h`, and `d`. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  shutdown_delay?: unknown;
  /** Servers is the list of servers, keyed by arbitrary names chosen at your discretion for your own convenience; the keys do not affect functionality. Server describes an HTTP server. */
  servers?: AppServers;
  /** If set, metrics observations will be enabled. This setting is EXPERIMENTAL and subject to change. Metrics configures metrics observations. EXPERIMENTAL and subject to change or removal. */
  metrics?: Metrics;
}
export const App = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    http_port: S.optional(S.Number),
    https_port: S.optional(S.Number),
    grace_period: S.optional(S.Unknown),
    shutdown_delay: S.optional(S.Unknown),
    servers: S.optional(AppServers),
    metrics: S.optional(Metrics),
  }),
).annotate({ identifier: "App" }) as any as S.Schema<App>;

/** Which subjects (hostnames or IP addresses) this policy applies to. This list is a filter, not a command. In other words, it is used only to filter whether this policy should apply to a subject that needs a certificate; it does NOT command the TLS app to manage a certificate for that subject. To have Caddy automate a certificate or specific subjects, use the "automate" certificate loader module of the TLS app. */
export type AutomationPolicySubjects = Array<string>;
export const AutomationPolicySubjects = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AutomationPolicySubjects>;

/** The modules that may issue certificates. Default: internal if all subjects do not qualify for public certificates; otherwise acme and zerossl. */
export type AutomationPolicyIssuers = Array<unknown>;
export const AutomationPolicyIssuers = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<AutomationPolicyIssuers>;

/** Modules that can get a custom certificate to use for any given TLS handshake at handshake-time. Custom certificates can be useful if another entity is managing certificates and Caddy need only get it and serve it. Specifying a Manager enables on-demand TLS, i.e. it has the side-effect of setting the on_demand parameter to `true`. TODO: This is an EXPERIMENTAL feature. Subject to change or removal. */
export type AutomationPolicyGet_certificate = Array<unknown>;
export const AutomationPolicyGet_certificate = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<AutomationPolicyGet_certificate>;

/** Overrides the URLs of OCSP responders embedded in certificates. Each key is a OCSP server URL to override, and its value is the replacement. An empty value will disable querying of that server. EXPERIMENTAL. Subject to change. */
export type AutomationPolicyOcsp_overrides = {
  [key: string]: string | undefined;
};
export const AutomationPolicyOcsp_overrides = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<AutomationPolicyOcsp_overrides>;

/** The list of automation policies. The first policy matching a certificate or subject name will be applied. AutomationPolicy designates the policy for automating the management (obtaining, renewal, and revocation) of managed TLS certificates. An AutomationPolicy value is not valid until it has been provisioned; use the `AddAutomationPolicy()` method on the TLS app to properly provision a new policy. */
export interface AutomationPolicy {
  /** Which subjects (hostnames or IP addresses) this policy applies to. This list is a filter, not a command. In other words, it is used only to filter whether this policy should apply to a subject that needs a certificate; it does NOT command the TLS app to manage a certificate for that subject. To have Caddy automate a certificate or specific subjects, use the "automate" certificate loader module of the TLS app. */
  subjects?: AutomationPolicySubjects;
  /** The modules that may issue certificates. Default: internal if all subjects do not qualify for public certificates; otherwise acme and zerossl. */
  issuers?: AutomationPolicyIssuers;
  /** Modules that can get a custom certificate to use for any given TLS handshake at handshake-time. Custom certificates can be useful if another entity is managing certificates and Caddy need only get it and serve it. Specifying a Manager enables on-demand TLS, i.e. it has the side-effect of setting the on_demand parameter to `true`. TODO: This is an EXPERIMENTAL feature. Subject to change or removal. */
  get_certificate?: AutomationPolicyGet_certificate;
  /** If true, certificates will be requested with MustStaple. Not all CAs support this, and there are potentially serious consequences of enabling this feature without proper threat modeling. */
  must_staple?: boolean;
  /** How long before a certificate's expiration to try renewing it, as a function of its total lifetime. As a general and conservative rule, it is a good idea to renew a certificate when it has about 1/3 of its total lifetime remaining. This utilizes the majority of the certificate's lifetime while still saving time to troubleshoot problems. However, for extremely short-lived certs, you may wish to increase the ratio to ~1/2. Unrecognized structure-API node type "float" — left untyped; see docs/provenance.md. */
  renewal_window_ratio?: unknown;
  /** The type of key to generate for certificates. Supported values: `ed25519`, `p256`, `p384`, `rsa2048`, `rsa4096`. */
  key_type?: string;
  /** Optionally configure a separate storage module associated with this manager, instead of using Caddy's global/default-configured storage. Module — the name is one of com.caddy.admin#CaddyStorageModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  storage?: unknown;
  /** If true, certificates will be managed "on demand"; that is, during TLS handshakes or when needed, as opposed to at startup or config load. This enables On-Demand TLS for this policy. */
  on_demand?: boolean;
  /** If true, private keys already existing in storage will be reused. Otherwise, a new key will be created for every new certificate to mitigate pinning and reduce the scope of key compromise. TEMPORARY: Key pinning is against industry best practices. This property will likely be removed in the future. Do not rely on it forever; watch the release notes. */
  reuse_private_keys?: boolean;
  /** Disables OCSP stapling. Disabling OCSP stapling puts clients at greater risk, reduces their privacy, and usually lowers client performance. It is NOT recommended to disable this unless you are able to justify the costs. EXPERIMENTAL. Subject to change. */
  disable_ocsp_stapling?: boolean;
  /** Overrides the URLs of OCSP responders embedded in certificates. Each key is a OCSP server URL to override, and its value is the replacement. An empty value will disable querying of that server. EXPERIMENTAL. Subject to change. */
  ocsp_overrides?: AutomationPolicyOcsp_overrides;
}
export const AutomationPolicy = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    subjects: S.optional(AutomationPolicySubjects),
    issuers: S.optional(AutomationPolicyIssuers),
    get_certificate: S.optional(AutomationPolicyGet_certificate),
    must_staple: S.optional(S.Boolean),
    renewal_window_ratio: S.optional(S.Unknown),
    key_type: S.optional(S.String),
    storage: S.optional(S.Unknown),
    on_demand: S.optional(S.Boolean),
    reuse_private_keys: S.optional(S.Boolean),
    disable_ocsp_stapling: S.optional(S.Boolean),
    ocsp_overrides: S.optional(AutomationPolicyOcsp_overrides),
  }),
).annotate({
  identifier: "AutomationPolicy",
}) as any as S.Schema<AutomationPolicy>;

/** The list of automation policies. The first policy matching a certificate or subject name will be applied. AutomationPolicy designates the policy for automating the management (obtaining, renewal, and revocation) of managed TLS certificates. An AutomationPolicy value is not valid until it has been provisioned; use the `AddAutomationPolicy()` method on the TLS app to properly provision a new policy. */
export type AutomationConfigPolicies = Array<AutomationPolicy>;
export const AutomationConfigPolicies = /*@__PURE__*/ S.Array(
  AutomationPolicy,
) as any as S.Schema<AutomationConfigPolicies>;

/** On-Demand TLS defers certificate operations to the moment they are needed, e.g. during a TLS handshake. Useful when you don't know all the hostnames at config-time, or when you are not in control of the domain names you are managing certificates for. In 2015, Caddy became the first web server to implement this experimental technology. Note that this field does not enable on-demand TLS; it only configures it for when it is used. To enable it, create an automation policy with `on_demand`. OnDemandConfig configures on-demand TLS, for obtaining needed certificates at handshake-time. Because this feature can easily be abused, Caddy must ask permission to your application whether a particular domain is allowed to have a certificate issued for it. */
export interface OnDemandConfig {
  /** Deprecated. WILL BE REMOVED SOON. Use 'permission' instead with the `http` module. */
  ask?: string;
  /** REQUIRED. A module that will determine whether a certificate is allowed to be loaded from storage or obtained from an issuer on demand. Module — the name is one of com.caddy.admin#TlsPermissionModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  permission?: unknown;
}
export const OnDemandConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ask: S.optional(S.String),
    permission: S.optional(S.Unknown),
  }),
).annotate({ identifier: "OnDemandConfig" }) as any as S.Schema<OnDemandConfig>;

/** Configures certificate automation. AutomationConfig governs the automated management of TLS certificates. */
export interface AutomationConfig {
  /** The list of automation policies. The first policy matching a certificate or subject name will be applied. AutomationPolicy designates the policy for automating the management (obtaining, renewal, and revocation) of managed TLS certificates. An AutomationPolicy value is not valid until it has been provisioned; use the `AddAutomationPolicy()` method on the TLS app to properly provision a new policy. */
  policies?: AutomationConfigPolicies;
  /** On-Demand TLS defers certificate operations to the moment they are needed, e.g. during a TLS handshake. Useful when you don't know all the hostnames at config-time, or when you are not in control of the domain names you are managing certificates for. In 2015, Caddy became the first web server to implement this experimental technology. Note that this field does not enable on-demand TLS; it only configures it for when it is used. To enable it, create an automation policy with `on_demand`. OnDemandConfig configures on-demand TLS, for obtaining needed certificates at handshake-time. Because this feature can easily be abused, Caddy must ask permission to your application whether a particular domain is allowed to have a certificate issued for it. */
  on_demand?: OnDemandConfig;
  /** Caddy staples OCSP (and caches the response) for all qualifying certificates by default. This setting changes how often it scans responses for freshness, and updates them if they are getting stale. Default: 1h Duration can be an integer or a string. An integer is interpreted as nanoseconds. If a string, it is a Go time.Duration value such as `300ms`, `1.5h`, or `2h45m`; valid units are `ns`, `us`/`µs`, `ms`, `s`, `m`, `h`, and `d`. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  ocsp_interval?: unknown;
  /** Every so often, Caddy will scan all loaded, managed certificates for expiration. This setting changes how frequently the scan for expiring certificates is performed. Default: 10m Duration can be an integer or a string. An integer is interpreted as nanoseconds. If a string, it is a Go time.Duration value such as `300ms`, `1.5h`, or `2h45m`; valid units are `ns`, `us`/`µs`, `ms`, `s`, `m`, `h`, and `d`. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  renew_interval?: unknown;
  /** How often to scan storage units for old or expired assets and remove them. These scans exert lots of reads (and list operations) on the storage module, so choose a longer interval for large deployments. Default: 24h Storage will always be cleaned when the process first starts. Then, a new cleaning will be started this duration after the previous cleaning started if the previous cleaning finished in less than half the time of this interval (otherwise next start will be skipped). Duration can be an integer or a string. An integer is interpreted as nanoseconds. If a string, it is a Go time.Duration value such as `300ms`, `1.5h`, or `2h45m`; valid units are `ns`, `us`/`µs`, `ms`, `s`, `m`, `h`, and `d`. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  storage_clean_interval?: unknown;
}
export const AutomationConfig = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    policies: S.optional(AutomationConfigPolicies),
    on_demand: S.optional(OnDemandConfig),
    ocsp_interval: S.optional(S.Unknown),
    renew_interval: S.optional(S.Unknown),
    storage_clean_interval: S.optional(S.Unknown),
  }),
).annotate({
  identifier: "AutomationConfig",
}) as any as S.Schema<AutomationConfig>;

/** Configures session ticket ephemeral keys (STEKs). SessionTicketService configures and manages TLS session tickets. */
export interface SessionTicketService {
  /** KeySource is the method by which Caddy produces or obtains TLS session ticket keys (STEKs). By default, Caddy generates them internally using a secure pseudorandom source. Module — the name is one of com.caddy.admin#TlsStekModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  key_source?: unknown;
  /** How often Caddy rotates STEKs. Default: 12h. Duration can be an integer or a string. An integer is interpreted as nanoseconds. If a string, it is a Go time.Duration value such as `300ms`, `1.5h`, or `2h45m`; valid units are `ns`, `us`/`µs`, `ms`, `s`, `m`, `h`, and `d`. A Go time.Duration: an integer (nanoseconds) OR a duration string like "10s". Document — Smithy has no int-or-string scalar. */
  rotation_interval?: unknown;
  /** The maximum number of keys to keep in rotation. Default: 4. */
  max_keys?: number;
  /** Disables STEK rotation. */
  disable_rotation?: boolean;
  /** Disables TLS session resumption by tickets. */
  disabled?: boolean;
}
export const SessionTicketService = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    key_source: S.optional(S.Unknown),
    rotation_interval: S.optional(S.Unknown),
    max_keys: S.optional(S.Number),
    disable_rotation: S.optional(S.Boolean),
    disabled: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "SessionTicketService",
}) as any as S.Schema<SessionTicketService>;

/** Configures the in-memory certificate cache. CertCacheOptions configures the certificate cache. */
export interface CertCacheOptions {
  /** Maximum number of certificates to allow in the cache. If reached, certificates will be randomly evicted to make room for new ones. Default: 10,000 */
  capacity?: number;
}
export const CertCacheOptions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    capacity: S.optional(S.Number),
  }),
).annotate({
  identifier: "CertCacheOptions",
}) as any as S.Schema<CertCacheOptions>;

/** TLS provides TLS facilities including certificate loading and management, client auth, and more. */
export interface TLS {
  /** Certificates to load into memory for quick recall during TLS handshakes. Each key is the name of a certificate loader module. The "automate" certificate loader module can be used to specify a list of subjects that need certificates to be managed automatically. The first matching automation policy will be applied to manage the certificate(s). All loaded certificates get pooled into the same cache and may be used to complete TLS handshakes for the relevant server names (SNI). Certificates loaded manually (anything other than "automate") are not automatically managed and will have to be refreshed manually before they expire. ModuleMap is a map that can contain multiple modules, where the map key is the module's name. (The namespace is usually read from an associated field's struct tag.) Because the module's name is given as the key in a module map, the name does not have to be given in the json.RawMessage. Module map — the name is one of com.caddy.admin#TlsCertificatesModuleName's members on this pinned binary, or a name outside this snapshot (module registry can grow); its own fields are not modeled (see docs/provenance.md). */
  certificates?: unknown;
  /** Configures certificate automation. AutomationConfig governs the automated management of TLS certificates. */
  automation?: AutomationConfig;
  /** Configures session ticket ephemeral keys (STEKs). SessionTicketService configures and manages TLS session tickets. */
  session_tickets?: SessionTicketService;
  /** Configures the in-memory certificate cache. CertCacheOptions configures the certificate cache. */
  cache?: CertCacheOptions;
  /** Disables OCSP stapling for manually-managed certificates only. To configure OCSP stapling for automated certificates, use an automation policy instead. Disabling OCSP stapling puts clients at greater risk, reduces their privacy, and usually lowers client performance. It is NOT recommended to disable this unless you are able to justify the costs. EXPERIMENTAL. Subject to change. */
  disable_ocsp_stapling?: boolean;
  /** Disables checks in certmagic that the configured storage is ready and able to handle writing new content to it. These checks are intended to prevent information loss (newly issued certificates), but can be expensive on the storage. Disabling these checks should only be done when the storage can be trusted to have enough capacity and no other problems. EXPERIMENTAL. Subject to change. */
  disable_storage_check?: boolean;
  /** Disables the automatic cleanup of the storage backend. This is useful when TLS is not being used to store certificates and the user wants run their server in a read-only mode. Storage cleaning creates two files: instance.uuid and last_clean.json. The instance.uuid file is used to identify the instance of Caddy in a cluster. The last_clean.json file is used to store the last time the storage was cleaned. EXPERIMENTAL. Subject to change. */
  disable_storage_clean?: boolean;
}
export const TLS = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    certificates: S.optional(S.Unknown),
    automation: S.optional(AutomationConfig),
    session_tickets: S.optional(SessionTicketService),
    cache: S.optional(CertCacheOptions),
    disable_ocsp_stapling: S.optional(S.Boolean),
    disable_storage_check: S.optional(S.Boolean),
    disable_storage_clean: S.optional(S.Boolean),
  }),
).annotate({ identifier: "TLS" }) as any as S.Schema<TLS>;

/** AppsRaw — apps registered by name. Every key here is one this pinned binary actually registers (com.caddy.admin#AppModuleName); an app name outside that list is also legal Caddy config (the module registry can grow) and is simply not a member of this structure — see docs/provenance.md. */
export interface Apps {
  /** Registered on this binary (module "events") but not structurally modeled — no snapshot was taken for it. */
  events?: unknown;
  /** The http app. */
  http?: App;
  /** Registered on this binary (module "pki") but not structurally modeled — no snapshot was taken for it. */
  pki?: unknown;
  /** The tls app. */
  tls?: TLS;
  /** Registered on this binary (module "layer4") but not structurally modeled — no snapshot was taken for it. */
  layer4?: unknown;
  /** Registered on this binary (module "security") but not structurally modeled — no snapshot was taken for it. */
  security?: unknown;
}
export const Apps = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    events: S.optional(S.Unknown),
    http: S.optional(App),
    pki: S.optional(S.Unknown),
    tls: S.optional(TLS),
    layer4: S.optional(S.Unknown),
    security: S.optional(S.Unknown),
  }),
).annotate({ identifier: "Apps" }) as any as S.Schema<Apps>;

/** Config is the top (or beginning) of the Caddy configuration structure. Caddy config is expressed natively as a JSON document. If you prefer not to work with JSON directly, there are [many config adapters](/docs/config-adapters) available that can convert various inputs into Caddy JSON. Many parts of this config are extensible through the use of Caddy modules. Fields which have a json.RawMessage type and which appear as dots (•••) in the online docs can be fulfilled by modules in a certain module namespace. The docs show which modules can be used in a given place. Whenever a module is used, its name must be given either inline as part of the module, or as the key to the module's value. The docs will make it clear which to use. Generally, all config settings are optional, as it is Caddy convention to have good, documented default values. If a parameter is required, the docs should say so. Go programs which are directly building a Config struct value should take care to populate the JSON-encodable fields of the struct (i.e. the fields with `json` struct tags) if employing the module lifecycle (e.g. Provision method calls). */
export interface Config {
  /** AdminConfig configures Caddy's API endpoint, which is used to manage Caddy while it is running. */
  admin?: AdminConfig;
  /** Logging facilitates logging within Caddy. The default log is called "default" and you can customize it. You can also define additional logs. By default, all logs at INFO level and higher are written to standard error ("stderr" writer) in a human-readable format ("console" encoder if stdout is an interactive terminal, "json" encoder otherwise). All defined logs accept all log entries by default, but you can filter by level and module/logger names. A logger's name is the same as the module's name, but a module may append to logger names for more specificity. For example, you can filter logs emitted only by HTTP handlers using the name "http.handlers", because all HTTP handler module names have that prefix. Caddy logs (except the sink) are zero-allocation, so they are very high-performing in terms of memory and CPU time. Enabling sampling can further increase throughput on extremely high-load servers. */
  logging?: Logging;
  /** Module — the name is one of com.caddy.admin#CaddyStorageModuleName's members on this pinned binary (only `file_system` on the mini's build); its own fields are not modeled. Storage backends beyond this binary's build (e.g. `tls.certificates.load_storage` pointing elsewhere) cannot be expressed here — see docs/provenance.md. */
  storage?: unknown;
  /** AppsRaw are the apps that Caddy will load and run. The app module name is the key, and the app's config is the associated value. ModuleMap is a map that can contain multiple modules, where the map key is the module's name. (The namespace is usually read from an associated field's struct tag.) Because the module's name is given as the key in a module map, the name does not have to be given in the json.RawMessage. */
  apps?: Apps;
}
export const Config = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    admin: S.optional(AdminConfig),
    logging: S.optional(Logging),
    storage: S.optional(S.Unknown),
    apps: S.optional(Apps),
  }),
).annotate({ identifier: "Config" }) as any as S.Schema<Config>;

export type AdaptConfigError = CaddyOpError;
/** Adapts a configuration to Caddy JSON without loading or running it — side-effect free, so this is what plan-time validation uses. */
export const adaptConfig: API.OperationMethod<
  AdaptRequest,
  AdaptResponse,
  AdaptConfigError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AdaptRequest,
  output: AdaptResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "AdaptConfig",
}));

export type CaCertificatesError = CaddyOpError;
/** Returns the certificate chain of a particular PKI app CA by its id, as PEM text (used internally by `caddy trust`). */
export const caCertificates: API.OperationMethod<
  CaCertificatesRequest,
  CaCertificatesResponse,
  CaCertificatesError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CaCertificatesRequest,
  output: CaCertificatesResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "CaCertificates",
}));

export type CaInfoError = CaddyOpError;
/** Returns information about a particular PKI app CA by its id. */
export const caInfo: API.OperationMethod<
  CaInfoRequest,
  CaInfoResponse,
  CaInfoError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CaInfoRequest,
  output: CaInfoResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "CaInfo",
}));

export type CreateConfigError = CaddyOpError;
/** Strictly creates a new value at `path` — 409 if a key already exists there; inserts at an array index. */
export const createConfig: API.OperationMethod<
  MutateConfigRequest,
  CreateConfigResponse,
  CreateConfigError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: MutateConfigRequest,
  output: CreateConfigResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "CreateConfig",
}));

export type DeleteConfigError = CaddyOpError;
/** Removes the value at `path`. `path: ""` unloads the entire running configuration but leaves the process alive. */
export const deleteConfig: API.OperationMethod<
  DeleteConfigRequest,
  DeleteConfigResponse,
  DeleteConfigError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteConfigRequest,
  output: DeleteConfigResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "DeleteConfig",
}));

export type GetConfigError = CaddyOpError;
/** Exports Caddy's current configuration at the named path. */
export const getConfig: API.OperationMethod<
  GetConfigRequest,
  GetConfigResponse,
  GetConfigError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetConfigRequest,
  output: GetConfigResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "GetConfig",
}));

export type GetConfigByIdError = CaddyOpError;
/** Exports the config at an `"@id"`-tagged scope — the same as GetConfig via that object's expanded path. */
export const getConfigById: API.OperationMethod<
  IdConfigRequest,
  GetConfigResponse,
  GetConfigByIdError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: IdConfigRequest,
  output: GetConfigResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "GetConfigById",
}));

export type GetReverseProxyUpstreamsError = CaddyOpError;
/** Returns the current status of the configured reverse proxy upstreams (backends) — cross-check `fails`/`num_requests` against the handler's health-check config to judge availability. */
export const getReverseProxyUpstreams: API.OperationMethod<
  GetReverseProxyUpstreamsRequest,
  GetReverseProxyUpstreamsResponse,
  GetReverseProxyUpstreamsError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetReverseProxyUpstreamsRequest,
  output: GetReverseProxyUpstreamsResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "GetReverseProxyUpstreams",
}));

export type LoadConfigError = CaddyOpError;
/** Sets Caddy's configuration, overriding any previous configuration. Zero-downtime; if the new config fails for any reason, the old config is rolled back into place. ⚠️ A refused load can still answer 200 — see LoadRefused in ../../src/errors.ts. */
export const loadConfig: API.OperationMethod<
  LoadRequest,
  LoadResponse,
  LoadConfigError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: LoadRequest,
  output: LoadResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "LoadConfig",
}));

export type ReplaceConfigError = CaddyOpError;
/** Strictly replaces the existing value at `path` — 404 if nothing is there yet. */
export const replaceConfig: API.OperationMethod<
  MutateConfigRequest,
  ReplaceConfigResponse,
  ReplaceConfigError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: MutateConfigRequest,
  output: ReplaceConfigResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "ReplaceConfig",
}));

export type SetConfigError = CaddyOpError;
/** Sets/replaces the object at `path`, or appends to it if it is an array. */
export const setConfig: API.OperationMethod<
  MutateConfigRequest,
  SetConfigResponse,
  SetConfigError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: MutateConfigRequest,
  output: SetConfigResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "SetConfig",
}));

export type SetConfigByIdError = CaddyOpError;
/** Sets/appends at an `"@id"`-tagged scope — the same as SetConfig via that object's expanded path. */
export const setConfigById: API.OperationMethod<
  SetIdConfigRequest,
  SetConfigByIdResponse,
  SetConfigByIdError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SetIdConfigRequest,
  output: SetConfigByIdResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "SetConfigById",
}));

export type StopServerError = CaddyOpError;
/** Gracefully shuts down the server and exits the process. To stop only the running configuration, use DeleteConfig at `/config/`. */
export const stopServer: API.OperationMethod<
  StopServerRequest,
  StopServerResponse,
  StopServerError,
  CaddyOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StopServerRequest,
  output: StopServerResponse,
  errors: [UnknownCaddyError],
  protocol: CaddyProtocol,
  retry: Retry.Retry,
  operationName: "StopServer",
}));
