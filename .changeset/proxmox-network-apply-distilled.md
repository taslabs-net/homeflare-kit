---
'@homeflare/distilled-proxmox': patch
'@homeflare/alchemy': patch
---

Keep the PVE network `changes` sibling through the distilled protocol, and move NetworkApply onto named operations without storing the diff.
