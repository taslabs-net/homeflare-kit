---
---

Release tooling only: the alchemy consumer smoke test now installs a same-release interim SDK from its packed tarball when npm does not have that version yet, so a Version Packages PR that bumps both no longer fails (PR 195).
