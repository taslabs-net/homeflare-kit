---
'@homeflare/kit': minor
---

Initial release: `parseEnv` for typed, dependency-free environment parsing.

Bun-native toolchain (bun · oxfmt · oxlint · tsc), runtime-neutral output verified
against both bun and node by a consumer smoke test that installs the packed tarball.
